// @generated by idiolect-codegen. do not edit.
// source: pub.layers.media.defs

import type { DeviceInfo, FeatureMap, KnowledgeRef, ObjectRef, ReproducibilityInfo, Uuid } from "./../defs";

/**
* Composable audio metadata. Attach to any media record representing audio content: standalone audio files, audio tracks in video, etc.
*/
export interface AudioInfo {
  /**
  * Audio bit depth (e.g., 16, 24, 32).
  */
  bitDepth?: number;
  /**
  * Audio bitrate in bits per second.
  */
  bitRate?: number;
  /**
  * Bitrate mode.
  */
  bitRateMode?: AudioInfoBitRateMode;
  /**
  * Number of audio channels.
  */
  channels?: number;
  /**
  * Audio codec identifier (e.g., 'pcm_s16le', 'aac', 'opus', 'flac').
  */
  codec?: string;
  /**
  * Total number of audio samples. Enables sample-accurate alignment.
  */
  numberOfSamples?: number;
  /**
  * Audio sample rate in Hz (e.g., 8000, 16000, 22050, 44100, 48000).
  */
  sampleRate?: number;
  /**
  * AT-URI of a pub.layers.segmentation record structuring the transcript.
  */
  segmentationRef?: string;
  /**
  * Number of distinct speakers (for spoken language data).
  */
  speakerCount?: number;
  /**
  * AT-URI of a pub.layers.expression containing the transcript.
  */
  transcriptRef?: string;
}

/**
* The frame in which sensor, landmark, or volume coordinates are expressed. BIDS coordsystem.json, which requires a named system plus units and RECOMMENDS FiducialsCoordinates and AnatomicalLandmarkCoordinates. One def for all four use sites, so the four divergent lists the proposals carried collapse to one.
*/
export interface CoordinateSystem {
  /**
  * Free-text note about this coordinate frame.
  */
  description?: string;
  /**
  * Named landmark points defining the frame. BIDS FiducialsCoordinates and AnatomicalLandmarkCoordinates.
  */
  fiducials?: NamedPoint[];
  /**
  * AT-URI of a pub.layers.media.media of kind volume or image this frame is defined against. BIDS IntendedFor, a filesystem path upstream.
  */
  referenceImageRef?: string;
  /**
  * Direction code naming the positive direction of X, Y, Z (fallback when spatialAxesUri unavailable). BIDS Motion SpatialAxes. Determines how every coordinate in the record is read, so a consumer that cannot resolve it MUST NOT assume a default.
  */
  spatialAxes?: CoordinateSystemSpatialAxes;
  /**
  * AT-URI of the axis convention definition node (typeDef of `spatial-axes`).
  */
  spatialAxesUri?: string;
  /**
  * Coordinate system slug (fallback when systemUri unavailable). Covers the BIDS EEG, MEG and iEEG coordinate system lists plus the stereotaxic spaces used for volumetric data.
  */
  system: CoordinateSystemSystem;
  /**
  * AT-URI of the coordinate system definition node (typeDef of `coordinate-system`). Values MUST agree with pub.layers.defs#spatialEntity.crs.
  */
  systemUri?: string;
  /**
  * Unit of the stored integer coordinates (fallback when unitUri unavailable). BIDS coordinate units are m, mm or cm, plus pixels for iEEG on a photograph; nanometre is the integer-safe encoding of all three.
  */
  unit?: CoordinateSystemUnit;
  /**
  * AT-URI of the coordinate unit definition node.
  */
  unitUri?: string;
}

/**
* Composable document/image metadata. Attach to any media record representing scanned documents, manuscripts, printed text, or other page-based media for OCR/HTR annotation workflows.
*/
export interface DocumentInfo {
  /**
  * Scan color mode.
  */
  colorMode?: DocumentInfoColorMode;
  /**
  * Scanning resolution in dots per inch (300+ recommended for OCR).
  */
  dpi?: number;
  /**
  * OCR/HTR engine identifier (e.g., 'tesseract-5.3', 'transkribus', 'abbyy', 'google-vision').
  */
  ocrEngine?: string;
  /**
  * Number of pages in the document.
  */
  pageCount?: number;
  /**
  * Writing system (ISO 15924 codes: 'Latn', 'Arab', 'Deva', 'Hans', 'Hant', 'Cyrl', 'Grek', etc.).
  */
  scriptSystem?: string;
  /**
  * Primary text direction slug (fallback when writingDirectionUri unavailable).
  */
  writingDirection?: DocumentInfoWritingDirection;
  /**
  * AT-URI of the writing-direction definition node (typeDef of `writing-direction` at layers-core.ontology.layers.pub). Refines writingDirection; never replaces it.
  */
  writingDirectionUri?: string;
}

/**
* One trigger code and what it means. Grounds BIDS events.tsv trial_type, a bare string upstream, in a type definition node.
*/
export interface EventCode {
  /**
  * The channel this code appears on, when the recording has several trigger lines.
  */
  channel?: ObjectRef;
  /**
  * Integer value written on the trigger channel.
  */
  code: number;
  /**
  * Free-text note about this trigger code.
  */
  description?: string;
  /**
  * External grounding, for instance a HED tag (source hed) or a Cognitive Atlas concept.
  */
  knowledgeRef?: KnowledgeRef;
  /**
  * Condition label as the experimenter spells it.
  */
  label?: string;
  /**
  * AT-URI of the condition or event-type definition node, typically a pub.layers.ontology.typeDef.
  */
  typeUri?: string;
}

/**
* Composable eye-tracking metadata, carried alongside signalInfo. Screen geometry is deliberately absent: the same tracker at a different viewing distance yields different degrees of visual angle, so it is a property of stimulus presentation and lives on pub.layers.judgment.defs#presentationSpec.screen, which is where BIDS puts it (the StimulusPresentation object of the events sidecar).
*/
export interface EyeTrackingInfo {
  /**
  * BIDS AverageCalibrationError. Microdegrees because typical values are fractions of a degree.
  */
  averageCalibrationErrorMicrodegrees?: number;
  /**
  * BIDS CalibrationCount.
  */
  calibrationCount?: number;
  /**
  * BIDS CalibrationPosition.
  */
  calibrationPositionCount?: number;
  /**
  * BIDS CalibrationType (fallback when the Uri is unavailable), whose examples are H3 and HV9.
  */
  calibrationType?: EyeTrackingInfoCalibrationType;
  /**
  * AT-URI of the calibration grid definition node.
  */
  calibrationTypeUri?: string;
  /**
  * Eye-tracking hardware.
  */
  device?: DeviceInfo;
  /**
  * Algorithm used to derive fixations and saccades (fallback when the Uri is unavailable). Absent means the trace is raw and no events were derived.
  */
  eventDetection?: EyeTrackingInfoEventDetection;
  /**
  * AT-URI of the fixation and saccade detection algorithm definition node.
  */
  eventDetectionUri?: string;
  /**
  * BIDS MaximalCalibrationError.
  */
  maximalCalibrationErrorMicrodegrees?: number;
  /**
  * Tracking method slug (fallback when methodUri unavailable). BIDS EyeTrackingMethod, whose documented values include P-CR, DPI and EOG.
  */
  method?: EyeTrackingInfoMethod;
  /**
  * AT-URI of the eye-tracking method definition node.
  */
  methodUri?: string;
  /**
  * BIDS PupilFitMethod (fallback when the Uri is unavailable).
  */
  pupilFitMethod?: EyeTrackingInfoPupilFitMethod;
  /**
  * AT-URI of the pupil fit method definition node.
  */
  pupilFitMethodUri?: string;
  /**
  * Which eye was recorded (fallback when recordedEyeUri unavailable). BIDS RecordedEye, REQUIRED for eye-tracking recordings.
  */
  recordedEye?: EyeTrackingInfoRecordedEye;
  /**
  * AT-URI of the recorded-eye definition node.
  */
  recordedEyeUri?: string;
  /**
  * What the x and y samples mean (fallback when the Uri is unavailable). BIDS SampleCoordinateSystem, REQUIRED. gaze-on-screen is the value that makes presentationSpec.screen mandatory.
  */
  sampleCoordinateSystem?: EyeTrackingInfoSampleCoordinateSystem;
  /**
  * AT-URI of the gaze coordinate system definition node.
  */
  sampleCoordinateSystemUri?: string;
  /**
  * Unit of the gaze samples (fallback when the Uri is unavailable). BIDS SampleCoordinateUnits.
  */
  sampleCoordinateUnit?: EyeTrackingInfoSampleCoordinateUnit;
  /**
  * AT-URI of the gaze sample unit definition node.
  */
  sampleCoordinateUnitUri?: string;
  /**
  * BIDS EyeTrackerDistance.
  */
  trackerDistanceNanometres?: number;
}

/**
* One filter applied to a recording. BIDS makes SoftwareFilters REQUIRED and HardwareFilters RECOMMENDED and types both as arbitrary nested JSON, so a consumer cannot today decide whether a recording is admissible for a given analysis without reading prose. Every parameter here is an integer in a named unit.
*/
export interface FilterSpec {
  /**
  * Bandwidth of the stage, in millihertz.
  */
  bandwidthMilliHz?: number;
  /**
  * Centre frequency, for notch and bandstop stages, in millihertz.
  */
  centreMilliHz?: number;
  /**
  * For ica and ssp.
  */
  componentsRemoved?: number;
  /**
  * Free-text note about this filter stage.
  */
  description?: string;
  /**
  * Filter design slug (fallback when designUri unavailable).
  */
  design?: FilterSpecDesign;
  /**
  * AT-URI of the filter design definition node.
  */
  designUri?: string;
  /**
  * Whether the filter introduces phase shift (fallback when directionUri unavailable). Load-bearing for any latency claim about an ERP or evoked component.
  */
  direction?: FilterSpecDirection;
  /**
  * AT-URI of the filter direction definition node.
  */
  directionUri?: string;
  /**
  * Low-pass cutoff of this filter stage, in millihertz.
  */
  highCutoffMilliHz?: number;
  /**
  * High-pass cutoff of this filter stage, in millihertz.
  */
  lowCutoffMilliHz?: number;
  /**
  * Filter order.
  */
  order?: number;
  /**
  * Software that applied the filter, reusing the shared block rather than re-declaring name and version.
  */
  software?: ReproducibilityInfo;
  /**
  * Where the filter was applied (fallback when stageUri unavailable). Distinguishes BIDS HardwareFilters from SoftwareFilters.
  */
  stage?: FilterSpecStage;
  /**
  * AT-URI of the filter stage definition node.
  */
  stageUri?: string;
  /**
  * Filter type slug (fallback when typeUri unavailable).
  */
  type: FilterSpecType;
  /**
  * AT-URI of the filter type definition node.
  */
  typeUri?: string;
}

/**
* Composable still-image metadata. Added because 0.8.0 has no image block at all, so a photograph's width and height had to borrow videoInfo, whose own description says it is for video content.
*/
export interface ImageInfo {
  /**
  * Bit depth per channel.
  */
  bitDepth?: number;
  /**
  * Camera or scanner, structured rather than concatenated into a format string.
  */
  captureDevice?: DeviceInfo;
  /**
  * Image codec identifier (jpeg, png, webp, jp2, tiff-lzw). Wire-format code, exempt from pairing.
  */
  codec?: string;
  /**
  * Colour space slug (fallback when colorSpaceUri unavailable). Paired here even though the sibling videoInfo.colorSpace is not, since a new field should not inherit an existing wart.
  */
  colorSpace?: ImageInfoColorSpace;
  /**
  * AT-URI of the colour space definition node.
  */
  colorSpaceUri?: string;
  /**
  * Resolution in dots per inch, when the image derives from a physical original.
  */
  dpi?: number;
  /**
  * Height in pixels.
  */
  height?: number;
  /**
  * EXIF orientation tag.
  */
  orientation?: number;
  /**
  * Width in pixels.
  */
  width?: number;
}

/**
* Composable motion-capture and articulography metadata: optical marker systems, inertial suits, electromagnetic articulography, ultrasound tongue imaging, and body or hand pose streams. Follows BIDS Motion, in which a tracking system is a grouping level below the session, so one session routinely carries several media records at different rates. A motion stream also carries signalInfo, because in BIDS Motion a mocap channel is typed POS, ORNT, VEL, ACCEL, GYRO, MAGN, ANGACCEL or JNTANG, that is, it is a sampled signal.
*/
export interface MotionInfo {
  /**
  * Carries spatialAxes; a motion stream must not define a second axis vocabulary.
  */
  coordinateSystem?: CoordinateSystem;
  /**
  * Motion-capture hardware.
  */
  device?: DeviceInfo;
  /**
  * Container format slug (fallback when formatUri unavailable). Wire-format code.
  */
  format?: MotionInfoFormat;
  /**
  * AT-URI of the motion container format definition node.
  */
  formatUri?: string;
  /**
  * BIDS Motion RotationOrder (fallback when the Uri is unavailable).
  */
  rotationOrder?: MotionInfoRotationOrder;
  /**
  * AT-URI of the rotation order definition node.
  */
  rotationOrderUri?: string;
  /**
  * BIDS Motion RotationRule (fallback when the Uri is unavailable).
  */
  rotationRule?: MotionInfoRotationRule;
  /**
  * AT-URI of the rotation handedness definition node.
  */
  rotationRuleUri?: string;
  /**
  * Skeleton or marker-set slug (fallback when skeletonUri unavailable).
  */
  skeleton?: MotionInfoSkeleton;
  /**
  * AT-URI of the skeleton or marker-set definition node (typeDef of `skeleton`), which is where joint identity, joint order and joint topology are DEFINED. This is why no pose anchor is needed: the channels carry the numbers, this carries their meaning.
  */
  skeletonUri?: string;
  /**
  * Number of tracked points, markers, or joints.
  */
  trackedPointCount?: number;
  /**
  * The pub.layers.acquisition.defs#run or session stream grouping this system, as an objectRef. BIDS Motion tracksys entity, a bare label upstream.
  */
  trackingSystem?: ObjectRef;
}

/**
* A named point in the enclosing coordinate system, in that system's unit.
*/
export interface NamedPoint {
  /**
  * Landmark slug (fallback when nameUri unavailable).
  */
  name: NamedPointName;
  /**
  * AT-URI of the landmark definition node.
  */
  nameUri?: string;
  /**
  * Signed X coordinate of the point, in the enclosing system's unit.
  */
  x?: number;
  /**
  * Signed Y coordinate of the point, in the enclosing system's unit.
  */
  y?: number;
  /**
  * Signed Z coordinate of the point, in the enclosing system's unit.
  */
  z?: number;
}

/**
* Physical placement of one sensor: an EEG electrode, an iEEG contact, a MEG coil, an fNIRS optode, a motion marker, or an anatomical landmark. Coordinates are signed integer nanometres in the frame named by the enclosing coordinateSystem.
*/
export interface SensorSpec {
  /**
  * Grounded anatomical localization. NWB makes electrodes.location a required column and BIDS iEEG carries it free text; use source uberon. For iEEG this is a subject-specific claim about a subject-specific brain and inherits the participant's identifiability declaration.
  */
  anatomyRef?: KnowledgeRef;
  /**
  * BIDS iEEG electrodes.tsv size, a REQUIRED column, square millimetres upstream. Load-bearing for current-density interpretation.
  */
  contactAreaSquareMicrometres?: number;
  /**
  * Free-text note about this sensor.
  */
  description?: string;
  /**
  * Open-ended per-sensor features not captured by the typed fields.
  */
  features?: FeatureMap;
  /**
  * Electrode group or shaft. NWB ElectrodeGroup, whose name, description, location and device are all required there.
  */
  group?: ObjectRef;
  /**
  * Hemisphere slug (fallback when hemisphereUri unavailable). BIDS iEEG electrodes.tsv hemisphere.
  */
  hemisphere?: SensorSpecHemisphere;
  /**
  * AT-URI of the hemisphere definition node.
  */
  hemisphereUri?: string;
  /**
  * BIDS electrodes.tsv impedance; NWB electrodes.imp.
  */
  impedanceMilliOhm?: number;
  /**
  * Material slug (fallback when materialUri unavailable). BIDS electrodes.tsv material, free text upstream, where Ag/AgCl already has three predictable spellings.
  */
  material?: SensorSpecMaterial;
  /**
  * AT-URI of the electrode material definition node (typeDef of `electrode-material`).
  */
  materialUri?: string;
  /**
  * Sensor name exactly as in the raw file.
  */
  name: string;
  /**
  * Sensor type slug (fallback when typeUri unavailable). BIDS optodes.tsv type is REQUIRED and takes source or detector; the rest cover the electrode, coil, marker and landmark cases BIDS spreads across electrodes.tsv and coordsystem.json.
  */
  type: SensorSpecType;
  /**
  * AT-URI of the sensor type definition node (typeDef of `sensor-type`).
  */
  typeUri?: string;
  /**
  * Stable identifier within this record. Required because channels, anchors and spatial regions point at sensors.
  */
  uuid: Uuid;
  /**
  * Signed X. BIDS electrodes.tsv x and optodes.tsv x, REQUIRED columns; NWB electrodes.x.
  */
  xNanometres?: number;
  /**
  * Signed Y.
  */
  yNanometres?: number;
  /**
  * Signed Z.
  */
  zNanometres?: number;
}

/**
* One recorded channel. BIDS channels.tsv, whose name, type and units columns are REQUIRED for every electrophysiology modality and which additionally makes low_cutoff and high_cutoff REQUIRED for iEEG. Position is held by reference through sensor, because channel and sensor are not in one-to-one correspondence.
*/
export interface SignalChannel {
  /**
  * Free-text note about this channel.
  */
  description?: string;
  /**
  * Receiving optode. BIDS NIRS channels.tsv detector, a REQUIRED column.
  */
  detectorSensor?: ObjectRef;
  /**
  * Open-ended per-channel features not captured by the typed fields.
  */
  features?: FeatureMap;
  /**
  * Electrode group, shaft, grid, or tracking system this channel belongs to. NWB ElectrodeGroup; BIDS Motion tracksys.
  */
  group?: ObjectRef;
  /**
  * Low-pass cutoff. BIDS channels.tsv high_cutoff, a REQUIRED column for iEEG.
  */
  highCutoffMilliHz?: number;
  /**
  * High-pass cutoff. 0 means DC-coupled. BIDS channels.tsv low_cutoff, a REQUIRED column for iEEG.
  */
  lowCutoffMilliHz?: number;
  /**
  * Channel name exactly as in the raw file (Cz, MEG0113, S1-D1-760, gaze_x).
  */
  name: string;
  /**
  * BIDS channels.tsv notch.
  */
  notchMilliHz?: number;
  /**
  * Per-channel reference for bipolar and mixed intracranial montages. BIDS channels.tsv reference.
  */
  referenceChannel?: ObjectRef;
  /**
  * Per-channel rate when it differs from the recording's nominal rate, routine for auxiliary channels multiplexed into one file.
  */
  samplingFrequencyMilliHz?: number;
  /**
  * The sensors entry this channel reads from. How a channel acquires a position without duplicating coordinates per channel.
  */
  sensor?: ObjectRef;
  /**
  * Emitting optode. BIDS NIRS channels.tsv source, a REQUIRED column.
  */
  sourceSensor?: ObjectRef;
  /**
  * Channel quality slug (fallback when statusUri unavailable). BIDS channels.tsv status.
  */
  status?: SignalChannelStatus;
  /**
  * Why the channel is bad or interpolated. BIDS channels.tsv status_description.
  */
  statusDescription?: string;
  /**
  * AT-URI of the channel status definition node.
  */
  statusUri?: string;
  /**
  * Channel type slug (fallback when typeUri unavailable). Union of the BIDS keyword sets, uppercase as BIDS spells them.
  */
  type: SignalChannelType;
  /**
  * AT-URI of the channel type definition node (typeDef of `channel-type` at layers-acquisition.ontology.layers.pub). The authoritative field: the union of the BIDS EEG, MEG, iEEG, NIRS and Motion keyword sets already runs to roughly sixty terms and grows with each extension proposal. The knownValues below are a seed, not a closed list.
  */
  typeUri?: string;
  /**
  * Physical unit of this channel's samples (fallback when unitUri unavailable). BIDS channels.tsv units, REQUIRED, which expects SI units.
  */
  unit?: SignalChannelUnit;
  /**
  * AT-URI of the physical unit definition node (typeDef of `si-unit`).
  */
  unitUri?: string;
  /**
  * Stable identifier for this channel within this record. Required because anchors, reference declarations and sync declarations point at channels, and a name or a positional index silently mis-addresses when a republished recording drops two bad channels.
  */
  uuid: Uuid;
  /**
  * Nominal wavelength (760 nm = 760000). BIDS NIRS channels.tsv wavelength_nominal, a REQUIRED column.
  */
  wavelengthPicometres?: number;
}

/**
* Composable metadata for a sampled time-series recording. Attach to any media record of kind signal or motion. Frequencies are millihertz because BIDS types SamplingFrequency as number and 512.03 Hz occurs; lengths are nanometres because iEEG contact localization is reported sub-millimetre; times are session-relative nanoseconds, never UTC-epoch, since 2^53 ns is about 104 days.
*/
export interface SignalInfo {
  /**
  * AT-URI of a pub.layers.media.media holding the empty-room noise recording. BIDS AssociatedEmptyRoom is a filesystem path; an AT-URI is the structured form of the same relation.
  */
  associatedEmptyRoomRef?: string;
  /**
  * Total recorded channels. BIDS EEGChannelCount / MEGChannelCount / NIRSChannelCount. Authoritative when channels is not fully enumerated.
  */
  channelCount?: number;
  /**
  * Per-channel table in acquisition order. BIDS channels.tsv, whose name, type and units columns are REQUIRED. Inline rather than one record per channel: getRepo bulk export is fast and unmetered while listRecords caps at 100 per page, and a montage is one logical object.
  */
  channels?: SignalChannel[];
  /**
  * Cognitive Atlas term for the task. BIDS CogAtlasID is RECOMMENDED on every electrophysiology and behavioural sidecar. Use source cognitive-atlas.
  */
  cogAtlasRef?: KnowledgeRef;
  /**
  * BIDS ContinuousHeadLocalization.
  */
  continuousHeadLocalization?: boolean;
  /**
  * Frame in which sensors coordinates are expressed. BIDS coordsystem.json.
  */
  coordinateSystem?: CoordinateSystem;
  /**
  * BIDS NIRSDetectorOptodeCount, REQUIRED in the NIRS sidecar.
  */
  detectorOptodeCount?: number;
  /**
  * Acquisition hardware. BIDS Manufacturer / ManufacturersModelName / DeviceSerialNumber / SoftwareVersions; NWB Device.
  */
  device?: DeviceInfo;
  /**
  * MEG dewar position slug (fallback when dewarPositionUri unavailable). BIDS DewarPosition, REQUIRED in the MEG sidecar.
  */
  dewarPosition?: SignalInfoDewarPosition;
  /**
  * AT-URI of the dewar position definition node.
  */
  dewarPositionUri?: string;
  /**
  * BIDS DigitizedHeadPoints, REQUIRED in the MEG sidecar.
  */
  digitizedHeadPoints?: boolean;
  /**
  * BIDS DigitizedLandmarks, REQUIRED in the MEG sidecar.
  */
  digitizedLandmarks?: boolean;
  /**
  * Number of epochs, for epoched recordings.
  */
  epochCount?: number;
  /**
  * BIDS EpochLength. Absent for continuous recordings.
  */
  epochLengthNanos?: number;
  /**
  * The trigger or stimulus-code channel, as an objectRef into this record's channels.
  */
  eventChannel?: ObjectRef;
  /**
  * Trigger codes and their meanings. BIDS events.tsv carries trial_type as a bare string; grounding each code through a typeDef lets two labs' code 12 be recognized as the same condition or as different ones.
  */
  eventCodes?: EventCode[];
  /**
  * AT-URI of a pub.layers.annotation.annotationLayer of kind tier holding the decoded event stream. This is where BIDS events.tsv lands: onset and duration map to anchor.temporalSpan or anchor.signalSpan, trial_type to the layer subkind or the annotation label.
  */
  eventLayerRef?: string;
  /**
  * Open-ended features not captured by the typed fields.
  */
  features?: FeatureMap;
  /**
  * Whether filters is a complete account (fallback when filterStatusUri unavailable). BIDS makes SoftwareFilters REQUIRED and permits the literal n/a, which conflates none-applied with not-reported; separating them means an empty filters array is unambiguous.
  */
  filterStatus?: SignalInfoFilterStatus;
  /**
  * AT-URI of the filter-reporting status definition node.
  */
  filterStatusUri?: string;
  /**
  * Filters in application order. Carries BIDS HardwareFilters and SoftwareFilters, arbitrary untyped JSON upstream, as a typed list so a consumer can decide admissibility without parsing prose.
  */
  filters?: FilterSpec[];
  /**
  * Container format slug (fallback when formatUri unavailable). Names what a reader must open; mimeType names the wire type, which for most of these is application/octet-stream.
  */
  format?: SignalInfoFormat;
  /**
  * AT-URI of the container format definition node (typeDef of `signal-format` at layers-acquisition.ontology.layers.pub).
  */
  formatUri?: string;
  /**
  * The ground electrode. BIDS EEGGround / iEEGGround.
  */
  groundSensor?: ObjectRef;
  /**
  * BIDS HeadCircumference, centimetres upstream. On the recording rather than the participant because it is measured at acquisition.
  */
  headCircumferenceNanometres?: number;
  /**
  * BIDS MaxMovement.
  */
  maxMovementNanometres?: number;
  /**
  * Modality slug (fallback when modalityUri unavailable). Mirrors recordingMethod.method member for member, extended with the continuous physiological channels that vocabulary omits.
  */
  modality: SignalInfoModality;
  /**
  * AT-URI of the modality definition node: a pub.layers.ontology.typeDef of the `modality` ontology at layers-core.ontology.layers.pub, or a pub.layers.graph.graphNode. The SAME node set is named by pub.layers.judgment.defs#recordingMethod.methodUri and pub.layers.catalog.defs#contentSummary.modalityUri. Each modality node reaches its carrier node by parentTypeRef, so media.kindUri is the parent rather than a synonym. Refines modality; never replaces it.
  */
  modalityUri?: string;
  /**
  * Total samples per channel. With samplingFrequencyMilliHz this makes pub.layers.defs#signalSpan sample anchoring exact.
  */
  numberOfSamples?: number;
  /**
  * Placement scheme slug (fallback when placementSchemeUri unavailable). BIDS EEGPlacementScheme, free text upstream.
  */
  placementScheme?: SignalInfoPlacementScheme;
  /**
  * AT-URI of the electrode or sensor placement definition node (typeDef of `electrode-placement`).
  */
  placementSchemeUri?: string;
  /**
  * Mains frequency in millihertz (50000 or 60000). BIDS PowerLineFrequency, REQUIRED in every electrophysiology sidecar: no downstream filtering decision can be made without it.
  */
  powerLineFrequencyMilliHz?: number;
  /**
  * BIDS RecordingDuration, given in seconds upstream.
  */
  recordingDurationNanos?: number;
  /**
  * Recording continuity slug (fallback when recordingTypeUri unavailable). BIDS RecordingType, same three values.
  */
  recordingType?: SignalInfoRecordingType;
  /**
  * AT-URI of the recording continuity definition node.
  */
  recordingTypeUri?: string;
  /**
  * Free-text reference detail for schemes the slug vocabulary cannot express. Carries BIDS EEGReference verbatim when it does not map.
  */
  referenceDescription?: string;
  /**
  * Referencing scheme slug (fallback when referenceSchemeUri unavailable). BIDS EEGReference and iEEGReference are both REQUIRED and both free text upstream; typing the common cases while keeping referenceDescription for the rest is the improvement over the upstream string.
  */
  referenceScheme?: SignalInfoReferenceScheme;
  /**
  * AT-URI of the reference scheme definition node (typeDef of `eeg-reference-scheme`).
  */
  referenceSchemeUri?: string;
  /**
  * The reference electrode, as an objectRef whose objectId names a sensors[] uuid in this record. Replaces a free-text channel name, which cannot be validated and cannot be detected as dangling.
  */
  referenceSensor?: ObjectRef;
  /**
  * Nominal sampling frequency in millihertz (2048 Hz = 2048000). BIDS SamplingFrequency, REQUIRED in the EEG, MEG, iEEG and NIRS sidecars; NWB ElectricalSeries.rate.
  */
  samplingFrequencyMilliHz: number;
  /**
  * Physical sensor geometry. BIDS electrodes.tsv and optodes.tsv plus NWB's electrodes DynamicTable. Separate from channels because the two are not in bijection: an fNIRS channel is a source-detector-wavelength triple and a bipolar iEEG channel is a difference of two contacts.
  */
  sensors?: SensorSpec[];
  /**
  * BIDS NIRSSourceOptodeCount, REQUIRED in the NIRS sidecar.
  */
  sourceOptodeCount?: number;
}

/**
* How one media stream aligns to a session's shared time base. This is what makes multimodal Layers possible: the 0.8.0 chain is annotationLayer.expression (single, REQUIRED) to expression.mediaRef (single), so a session of two cameras, a lapel microphone, an EEG cap and an eye tracker is unrepresentable, and parentMediaRef with startOffsetMs expresses excerpt-of, a different relation.
*/
export interface SyncInfo {
  /**
  * How alignment was established (fallback when the Uri is unavailable). A consumer needs it to know how far to trust a cross-stream anchor: a hardware trigger and a clapperboard differ by three orders of magnitude.
  */
  clockSource?: SyncInfoClockSource;
  /**
  * AT-URI of the synchronization method definition node (typeDef of `sync-method`).
  */
  clockSourceUri?: string;
  /**
  * Signed measured drift against the session master. Independent oscillators diverge measurably over an hour, and zero is a claim rather than a safe default.
  */
  driftPartsPerBillion?: number;
  /**
  * Signed offset from session time zero to this stream's first sample. Session-relative, never UTC-epoch. Negative when the stream began before the origin, which is normal for a continuously running amplifier and which BIDS StartTime explicitly permits.
  */
  offsetNanos?: number;
  /**
  * AT-URI of the pub.layers.acquisition.session whose clock this stream is expressed against.
  */
  sessionRef: string;
  /**
  * The channel carrying the shared sync signal, when clockSource is shared-trigger.
  */
  syncChannel?: ObjectRef;
  /**
  * Worst-case alignment error. Bounds how finely a cross-stream anchor may honestly be read.
  */
  syncUncertaintyNanos?: number;
}

/**
* Composable video metadata. Attach to any media record representing video content.
*/
export interface VideoInfo {
  /**
  * Display aspect ratio (e.g., '16:9', '4:3', '1:1').
  */
  aspectRatio?: string;
  /**
  * Video bitrate in bits per second.
  */
  bitRate?: number;
  /**
  * Video codec identifier (e.g., 'h264', 'h265', 'vp9', 'av1', 'prores').
  */
  codec?: string;
  /**
  * Color space.
  */
  colorSpace?: VideoInfoColorSpace;
  /**
  * Frame rate scaled by 100 (e.g., 2997 = 29.97fps). Avoids floats.
  */
  frameRate?: number;
  /**
  * Height in pixels.
  */
  height?: number;
  /**
  * Scan type. Affects frame extraction for annotation.
  */
  scanType?: VideoInfoScanType;
  /**
  * Width in pixels.
  */
  width?: number;
}

/**
* Composable metadata for volumetric imaging: fMRI runs, structural scans, and derived masks or parcellations. Volumes almost always exceed the blob cap, so externalUri plus contentDigest is the normal carriage rather than the exception.
*/
export interface VolumeInfo {
  /**
  * Stereotaxic frame the voxel grid is expressed against.
  */
  coordinateSystem?: CoordinateSystem;
  /**
  * Scanner hardware.
  */
  device?: DeviceInfo;
  /**
  * Voxel grid extent along X.
  */
  dimX?: number;
  /**
  * Voxel grid extent along Y.
  */
  dimY?: number;
  /**
  * Voxel grid extent along Z.
  */
  dimZ?: number;
  /**
  * BIDS EchoTime.
  */
  echoTimeNanos?: number;
  /**
  * BIDS FlipAngle.
  */
  flipAngleMicrodegrees?: number;
  /**
  * Container format slug (fallback when formatUri unavailable). Wire-format code.
  */
  format?: VolumeInfoFormat;
  /**
  * AT-URI of the volume container format definition node.
  */
  formatUri?: string;
  /**
  * BIDS PhaseEncodingDirection (j, j-). Wire-format code.
  */
  phaseEncodingDirection?: string;
  /**
  * BIDS RepetitionTime.
  */
  repetitionTimeNanos?: number;
  /**
  * BIDS SliceEncodingDirection (k, k-). Wire-format code.
  */
  sliceEncodingDirection?: string;
  /**
  * Per-slice acquisition offsets within one TR. BIDS SliceTiming.
  */
  sliceTimingNanos?: number[];
  /**
  * Volumes in the run. Bounds pub.layers.defs#signalSpan.volumeIndexEnd.
  */
  volumeCount?: number;
  /**
  * Volume kind slug (fallback when the Uri is unavailable). BIDS suffix plus datatype.
  */
  volumeKind: VolumeInfoVolumeKind;
  /**
  * AT-URI of the volume kind definition node.
  */
  volumeKindUri?: string;
  /**
  * Voxel edge length along X, in nanometres.
  */
  voxelSizeXNanometres?: number;
  /**
  * Voxel edge length along Y, in nanometres.
  */
  voxelSizeYNanometres?: number;
  /**
  * Voxel edge length along Z, in nanometres.
  */
  voxelSizeZNanometres?: number;
}

export type AudioInfoBitRateMode = "cbr" | "vbr" | string & {};

export type CoordinateSystemSpatialAxes = "RAS" | "LAS" | "RAI" | "LPS" | "ARS" | "ALS" | "FRU" | "FLU" | "custom" | string & {};

export type CoordinateSystemSystem = "captrak" | "eeglab" | "ctf-head" | "ctf-device" | "elekta-neuromag-head" | "elekta-neuromag-device" | "bti-4d-head" | "acpc" | "scanner-ras" | "mni152-nlin-2009c" | "mni305" | "talairach" | "fsaverage" | "individual-t1" | "voxel-index" | "pixel" | "world-metric" | "custom" | string & {};

export type CoordinateSystemUnit = "nanometre" | "micrometre" | "pixel" | "voxel" | "per-mille-normalized" | string & {};

export type DocumentInfoColorMode = "color" | "grayscale" | "bitonal" | string & {};

export type DocumentInfoWritingDirection = "ltr" | "rtl" | "ttb" | "boustrophedon" | "custom" | string & {};

export type EyeTrackingInfoCalibrationType = "h3" | "hv5" | "hv9" | "hv13" | "none" | "custom" | string & {};

export type EyeTrackingInfoEventDetection = "velocity-threshold" | "dispersion-threshold" | "eyelink-parser" | "engbert-kliegl" | "none" | "custom" | string & {};

export type EyeTrackingInfoMethod = "p-cr" | "dpi" | "eog" | "video-oculography" | "scleral-search-coil" | "custom" | string & {};

export type EyeTrackingInfoPupilFitMethod = "ellipse" | "centroid" | "custom" | string & {};

export type EyeTrackingInfoRecordedEye = "left" | "right" | "both" | "cyclopean" | string & {};

export type EyeTrackingInfoSampleCoordinateSystem = "gaze-on-screen" | "eye-in-head" | "gaze-in-world" | "custom" | string & {};

export type EyeTrackingInfoSampleCoordinateUnit = "pixel" | "nanometre" | "microdegree" | "per-mille-normalized" | string & {};

export type FilterSpecDesign = "butterworth" | "chebyshev-1" | "chebyshev-2" | "elliptic" | "bessel" | "fir-windowed" | "fir-least-squares" | "custom" | string & {};

export type FilterSpecDirection = "causal" | "zero-phase" | "unknown" | string & {};

export type FilterSpecStage = "hardware" | "software" | "unknown" | string & {};

export type FilterSpecType = "highpass" | "lowpass" | "bandpass" | "bandstop" | "notch" | "detrend" | "resample" | "rereference" | "ica" | "ssp" | "regression" | "custom" | string & {};

export type ImageInfoColorSpace = "srgb" | "adobe-rgb" | "display-p3" | "rec2020" | "cmyk" | "grayscale" | "bitonal" | "custom" | string & {};

export type MotionInfoFormat = "c3d" | "bvh" | "trc" | "fbx" | "tsv" | "csv" | "custom" | string & {};

export type MotionInfoRotationOrder = "xyz" | "xzy" | "yxz" | "yzx" | "zxy" | "zyx" | "quaternion" | string & {};

export type MotionInfoRotationRule = "right-hand" | "left-hand" | string & {};

export type MotionInfoSkeleton = "coco-17" | "openpose-body25" | "openpose-135" | "mediapipe-holistic" | "mediapipe-hand-21" | "smpl" | "smplx" | "vicon-plug-in-gait" | "xsens-mvn" | "ema-tongue-jaw-lips" | "c3d-custom" | "custom" | string & {};

export type NamedPointName = "nasion" | "lpa" | "rpa" | "inion" | "ac" | "pc" | "vertex" | "custom" | string & {};

export type SensorSpecHemisphere = "left" | "right" | "midline" | "not-reported" | string & {};

export type SensorSpecMaterial = "ag-agcl" | "platinum" | "platinum-iridium" | "gold" | "tin" | "stainless-steel" | "silicon" | "custom" | string & {};

export type SensorSpecType = "electrode" | "depth-contact" | "grid-contact" | "strip-contact" | "magnetometer" | "gradiometer" | "reference-coil" | "head-position-coil" | "source" | "detector" | "marker" | "imu" | "camera" | "fiducial" | "anatomical-landmark" | "head-point" | "custom" | string & {};

export type SignalChannelStatus = "good" | "bad" | "interpolated" | "not-reported" | string & {};

export type SignalChannelType = "EEG" | "REF" | "EOG" | "HEOG" | "VEOG" | "ECG" | "EMG" | "GSR" | "PPG" | "RESP" | "TEMP" | "PUPIL" | "EYEGAZE" | "TRIG" | "SYSCLOCK" | "AUDIO" | "MISC" | "MEGMAG" | "MEGGRADAXIAL" | "MEGGRADPLANAR" | "MEGREFMAG" | "MEGREFGRADAXIAL" | "MEGREFGRADPLANAR" | "MEGOTHER" | "HLU" | "FITERR" | "ADC" | "DAC" | "SEEG" | "ECOG" | "DBS" | "NIRSCWAMPLITUDE" | "NIRSCWOPTICALDENSITY" | "NIRSCWHBO" | "NIRSCWHBR" | "ACCEL" | "GYRO" | "MAGN" | "ORNT" | "POS" | "VEL" | "ANGACCEL" | "JNTANG" | "LATENCY" | "CUSTOM" | string & {};

export type SignalChannelUnit = "volt" | "millivolt" | "microvolt" | "tesla" | "femtotesla" | "ampere" | "microampere" | "ohm" | "siemens" | "microsiemens" | "degree-celsius" | "pixel" | "millimetre" | "degree" | "microdegree" | "radian" | "metre-per-second-squared" | "radian-per-second" | "arbitrary" | "not-applicable" | "custom" | string & {};

export type SignalInfoDewarPosition = "upright" | "supine" | "degrees-45" | "custom" | string & {};

export type SignalInfoFilterStatus = "as-listed" | "none-applied" | "not-reported" | string & {};

export type SignalInfoFormat = "edf" | "edf-plus" | "bdf" | "fif" | "eeglab-set" | "brainvision" | "nwb" | "nix" | "snirf" | "asc" | "edf-eyelink" | "c3d" | "trc" | "bvh" | "tsv" | "csv" | "custom" | string & {};

export type SignalInfoModality = "eeg" | "meg" | "ieeg" | "ecog" | "fmri" | "fnirs" | "eye-tracking" | "pupillometry" | "emg" | "ecg" | "skin-conductance" | "respiration" | "mouse-tracking" | "keystroke" | "motion-capture" | "articulography" | "custom" | string & {};

export type SignalInfoPlacementScheme = "10-20" | "10-10" | "10-5" | "biosemi-32" | "biosemi-64" | "biosemi-128" | "egi-hydrocel-128" | "egi-hydrocel-256" | "ctf-275" | "neuromag-306" | "subdural-grid" | "subdural-strip" | "depth-electrode" | "mixed-intracranial" | "subject-specific" | "custom" | string & {};

export type SignalInfoRecordingType = "continuous" | "epoched" | "discontinuous" | string & {};

export type SignalInfoReferenceScheme = "single-electrode" | "linked-mastoids" | "linked-ears" | "average" | "rest" | "bipolar" | "laplacian" | "intracranial-common" | "hardware-common" | "not-applicable" | "custom" | string & {};

export type SyncInfoClockSource = "session-master" | "shared-trigger" | "ltc-timecode" | "clapperboard" | "audio-cross-correlation" | "ptp" | "ntp" | "manual" | "unsynchronized" | "unknown" | string & {};

export type VideoInfoColorSpace = "rgb" | "yuv420" | "yuv422" | "yuv444" | "grayscale" | string & {};

export type VideoInfoScanType = "progressive" | "interlaced" | string & {};

export type VolumeInfoFormat = "nifti-1" | "nifti-2" | "cifti-2" | "gifti" | "dicom" | "mgz" | "minc" | "custom" | string & {};

export type VolumeInfoVolumeKind = "bold" | "anatomical-t1" | "anatomical-t2" | "dwi" | "fieldmap" | "perfusion" | "mask" | "statistical-map" | "parcellation" | "custom" | string & {};
