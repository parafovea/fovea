// @generated by idiolect-codegen. do not edit.
// source: pub.layers.judgment.experimentDef

import type { FeatureMap, KnowledgeRef, Licensing, ReproducibilityInfo } from "./../defs";
import type { ExperimentDesign, PresentationSpec, RecordingMethod } from "././defs";

/**
* Definition of an annotation or judgment experiment.
*/
export interface ExperimentDef {
  /**
  * References to pub.layers.resource#collection records providing filler pools for this experiment.
  */
  collectionRefs?: string[];
  corpusRef?: string;
  createdAt: string;
  description?: string;
  /**
  * Experiment design parameters: list constraints, distribution strategy, item order.
  */
  design?: ExperimentDesign;
  /**
  * Eprint records (papers/preprints) describing or associated with this experiment.
  */
  eprintRefs?: string[];
  features?: FeatureMap;
  /**
  * Instructions or guidelines text shown to participants.
  */
  guidelines?: string;
  /**
  * Format of the guidelines text, so consumers can render it safely without sniffing. Defaults to plain when omitted.
  */
  guidelinesFormat?: ExperimentDefGuidelinesFormat;
  /**
  * Knowledge graph references (e.g., theoretical framework, methodology citation, task ontology).
  */
  knowledgeRefs?: KnowledgeRef[];
  /**
  * Available labels for categorical judgments.
  */
  labels?: string[];
  /**
  * Licensing terms governing this experiment definition (supports dual/multi/component licensing).
  */
  licensing?: Licensing;
  /**
  * What property or behavior is being measured (fallback when measureTypeUri unavailable).
  */
  measureType?: ExperimentDefMeasureType;
  /**
  * AT-URI of the measure type definition node. Community-expandable via knowledge graph.
  */
  measureTypeUri?: string;
  name: string;
  ontologyRef?: string;
  personaRef?: string;
  /**
  * How stimuli are displayed to participants.
  */
  presentation?: PresentationSpec;
  /**
  * Data capture instruments used in this experiment.
  */
  recordingMethods?: RecordingMethod[];
  /**
  * How this experiment dataset was produced (code, commit, command, environment, seed).
  */
  reproducibility?: ReproducibilityInfo;
  /**
  * Maximum scale value.
  */
  scaleMax?: number;
  /**
  * Minimum scale value for ordinal-scale judgments.
  */
  scaleMin?: number;
  /**
  * Response instrument: how the response is collected (fallback when taskTypeUri unavailable).
  */
  taskType?: ExperimentDefTaskType;
  /**
  * AT-URI of the response instrument definition node. Community-expandable via knowledge graph.
  */
  taskTypeUri?: string;
  /**
  * References to pub.layers.resource#template records used to generate stimuli for this experiment.
  */
  templateRefs?: string[];
}

export type ExperimentDefGuidelinesFormat = "plain" | "html" | "markdown" | string & {};

export type ExperimentDefMeasureType = "acceptability" | "inference" | "similarity" | "plausibility" | "comprehension" | "preference" | "extraction" | "reading-time" | "production" | "custom" | string & {};

export type ExperimentDefTaskType = "forced-choice" | "multi-select" | "ordinal-scale" | "magnitude" | "binary" | "categorical" | "free-text" | "cloze" | "span-labeling" | "custom" | string & {};
