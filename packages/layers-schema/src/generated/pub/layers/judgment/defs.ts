// @generated by idiolect-codegen. do not edit.
// source: pub.layers.judgment.defs

import type { Constraint, FeatureMap, ObjectRef, Span } from "./../defs";

// Shared object definitions for the judgment namespace.

/**
* Experiment design parameters for item distribution, ordering, and timing.
*/
export interface ExperimentDesign {
  /**
  * How items are distributed to annotators.
  */
  distributionStrategy?: ExperimentDesignDistributionStrategy;
  /**
  * AT-URI of the distribution strategy definition node. Community-expandable.
  */
  distributionStrategyUri?: string;
  features?: FeatureMap;
  /**
  * How items are ordered within a list.
  */
  itemOrder?: ExperimentDesignItemOrder;
  /**
  * AT-URI of the item order definition node. Community-expandable.
  */
  itemOrderUri?: string;
  /**
  * Constraints on item list construction (Latin square balancing, no-adjacent-same-condition, etc.).
  */
  listConstraints?: ListConstraint[];
  /**
  * Target timing per item in milliseconds.
  */
  timingMs?: number;
}

/**
* A single judgment about a linguistic item.
*/
export interface Judgment {
  /**
  * Behavioral signals: mouse movements, keystroke patterns, eye tracking data, scroll events, etc.
  */
  behavioralData?: FeatureMap;
  /**
  * Categorical judgment label.
  */
  categoricalValue?: string;
  confidence?: number;
  features?: FeatureMap;
  /**
  * Reference to the pub.layers.resource#filling that generated the item being judged.
  */
  fillingRef?: string;
  /**
  * Free-text response.
  */
  freeText?: string;
  /**
  * Reference to the item being judged. Use recordRef for the communication/annotation record, objectId for a specific object within it, localId for same-record references.
  */
  item: ObjectRef;
  /**
  * Response time in milliseconds.
  */
  responseTimeMs?: number;
  /**
  * Numeric response value (ordinal-scale rating, magnitude estimate, or rank position).
  */
  scalarValue?: number;
  /**
  * Selected text span for span-labeling tasks.
  */
  textSpan?: Span;
}

/**
* A constraint on item list construction for an experiment.
*/
export interface ListConstraint {
  /**
  * Optional formal constraint expression.
  */
  constraint?: Constraint;
  /**
  * Constraint kind slug.
  */
  kind: ListConstraintKind;
  /**
  * AT-URI of the constraint kind definition node. Community-expandable.
  */
  kindUri?: string;
  /**
  * Constraint parameters (e.g., minimum distance, number of lists).
  */
  parameters?: FeatureMap;
  /**
  * The item property this constraint operates on (e.g., 'condition', 'templateRef').
  */
  targetProperty?: string;
}

/**
* How stimuli are displayed to participants.
*/
export interface PresentationSpec {
  /**
  * How text is segmented for incremental presentation.
  */
  chunkingUnit?: PresentationSpecChunkingUnit;
  /**
  * Whether previous chunks remain visible during incremental presentation.
  */
  cumulative?: boolean;
  features?: FeatureMap;
  /**
  * Inter-stimulus interval in milliseconds.
  */
  isiMs?: number;
  /**
  * Masking character for non-cumulative displays (e.g., '-', '#').
  */
  maskChar?: string;
  /**
  * Presentation method (fallback when methodUri unavailable).
  */
  method?: PresentationSpecMethod;
  /**
  * AT-URI of the presentation method definition node. Community-expandable via knowledge graph.
  */
  methodUri?: string;
  /**
  * Per-chunk display duration in milliseconds.
  */
  timingMs?: number;
}

/**
* A data capture instrument used in an experiment.
*/
export interface RecordingMethod {
  features?: FeatureMap;
  /**
  * Recording method (fallback when methodUri unavailable).
  */
  method: RecordingMethodMethod;
  /**
  * AT-URI of the recording method definition node. Community-expandable via knowledge graph.
  */
  methodUri?: string;
}

export type ExperimentDesignDistributionStrategy = "latin-square" | "random" | "blocked" | "stratified" | "custom" | string & {};

export type ExperimentDesignItemOrder = "random-order" | "fixed-order" | "blocked" | "adaptive" | "custom" | string & {};

export type ListConstraintKind = "latin-square" | "no-adjacent-same-condition" | "balanced-frequency" | "minimum-distance" | "custom" | string & {};

export type PresentationSpecChunkingUnit = "word" | "character" | "morpheme" | "phrase" | "sentence" | "region" | "custom" | string & {};

export type PresentationSpecMethod = "rsvp" | "self-paced" | "whole-sentence" | "auditory" | "visual-world" | "masked-priming" | "cross-modal" | "naturalistic" | "gating" | "maze" | "boundary" | "moving-window" | "custom" | string & {};

export type RecordingMethodMethod = "button-box" | "keyboard" | "mouse-click" | "touchscreen" | "voice" | "eeg" | "meg" | "fmri" | "fnirs" | "eye-tracking" | "pupillometry" | "mouse-tracking" | "emg" | "skin-conductance" | "ecog" | "custom" | string & {};
