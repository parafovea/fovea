// @generated by idiolect-codegen. do not edit.
// source: pub.layers.annotation.clusterSet

import type { AnnotationMetadata, Licensing, ReproducibilityInfo } from "./../defs";
import type { Cluster } from "././defs";

/**
* Groups annotations into equivalence classes. Used for coreference resolution (entity clusters, situation clusters), bridging anaphora grouping, and any annotation clustering task.
*/
export interface ClusterSet {
  clusters: Cluster[];
  /**
  * Corpus these clusters span (for cross-document clustering).
  */
  corpusRef?: string;
  createdAt: string;
  /**
  * Eprint records (papers/preprints) describing or associated with this cluster set.
  */
  eprintRefs?: string[];
  /**
  * Primary expression context. Optional for cross-document clustering.
  */
  expression?: string;
  /**
  * For cross-document clustering: all expressions these clusters span.
  */
  expressionRefs?: string[];
  /**
  * Clustering kind slug (fallback when kindUri unavailable).
  */
  kind: ClusterSetKind;
  /**
  * AT-URI of the clustering kind definition node. Community-expandable via knowledge graph.
  */
  kindUri?: string;
  /**
  * The annotation layer whose annotations these clusters group.
  */
  layerRef?: string;
  /**
  * Licensing terms governing this cluster set (supports dual/multi/component licensing).
  */
  licensing?: Licensing;
  metadata?: AnnotationMetadata;
  /**
  * How this cluster set was produced (code, commit, command, environment, seed).
  */
  reproducibility?: ReproducibilityInfo;
}

export type ClusterSetKind = "coreference" | "situation-coreference" | "bridging" | "same-as" | "clustering" | "custom" | string & {};
