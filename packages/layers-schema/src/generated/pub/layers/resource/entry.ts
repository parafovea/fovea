// @generated by idiolect-codegen. do not edit.
// source: pub.layers.resource.entry

import type { AnnotationMetadata, FeatureMap, KnowledgeRef, LanguageRef } from "./../defs";
import type { MweComponent } from "././defs";

/**
* A linguistic resource entry: a lexical item, frame element filler, morphological paradigm cell, or any atomic unit in a structured linguistic collection. Abstract enough to represent bead LexicalItems, FrameNet lexical units, PropBank rolesets, WordNet synset members, morphological paradigm cells, etc.
*/
export interface Entry {
  /**
  * For multi-word expressions: the component words.
  */
  components?: MweComponent[];
  /**
  * When this entry record was created.
  */
  createdAt: string;
  /**
  * Open-ended features: pos, morphological features, frequency, register, etc.
  */
  features?: FeatureMap;
  /**
  * Surface form or string representation.
  */
  form: string;
  /**
  * Knowledge graph groundings (WordNet synset, FrameNet lexical unit, Wikidata, etc.).
  */
  knowledgeRefs?: KnowledgeRef[];
  /**
  * Structured language references (canonical BCP-47 tag plus optional script, region, variety, and role) grounding the languages this record covers.
  */
  languageRefs?: LanguageRef[];
  /**
  * BCP-47 language tags this record covers. Empty when language is unspecified or unknown.
  */
  languages?: string[];
  /**
  * Canonical/citation form.
  */
  lemma?: string;
  /**
  * Provenance: who created this entry, with what tool, under what persona.
  */
  metadata?: AnnotationMetadata;
  /**
  * MWE kind slug (fallback when mweKindUri unavailable).
  */
  mweKind?: EntryMweKind;
  /**
  * AT-URI of the MWE kind definition node. Community-expandable via knowledge graph.
  */
  mweKindUri?: string;
  /**
  * Reference to a pub.layers.ontology#typeDef classifying this entry.
  */
  ontologyTypeRef?: string;
  /**
  * AT-URI of the source record this entry was derived from (e.g., an annotation, another entry).
  */
  sourceRef?: string;
}

export type EntryMweKind = "compound" | "phrasal-verb" | "idiom" | "light-verb" | "named-entity" | "collocation" | "custom" | string & {};
