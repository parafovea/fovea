// @generated by idiolect-codegen. do not edit.
// source: pub.layers.resource.collection

import type { AnnotationMetadata, FeatureMap, KnowledgeRef, LanguageRef, Licensing } from "./../defs";

/**
* A named collection of linguistic resource entries. Abstract enough to represent bead Lexicons, FrameNet frame inventories, PropBank frame files, WordNet synsets, morphological paradigm tables, gazetteers, stop-word lists, etc.
*/
export interface Collection {
  /**
  * When this collection record was created.
  */
  createdAt: string;
  /**
  * Human-readable description of this collection's scope and contents.
  */
  description?: string;
  /**
  * Eprint records (papers/preprints) describing or associated with this collection.
  */
  eprintRefs?: string[];
  /**
  * Open-ended features describing this collection: size, domain, register, provenance notes, etc.
  */
  features?: FeatureMap;
  /**
  * Collection kind slug (fallback when kindUri unavailable).
  */
  kind?: CollectionKind;
  /**
  * AT-URI of the collection kind definition node. Community-expandable via knowledge graph.
  */
  kindUri?: string;
  /**
  * Knowledge graph groundings for this collection (e.g., a published resource release, an ontology node, Wikidata).
  */
  knowledgeRefs?: KnowledgeRef[];
  /**
  * Structured language references (canonical BCP-47 tag plus optional script, region, variety, and role) grounding the languages this record covers.
  */
  languageRefs?: LanguageRef[];
  /**
  * BCP-47 language tags this record covers. Empty when language is unspecified or unknown.
  */
  languages?: string[];
  /**
  * Licensing terms governing this collection (supports dual/multi/component licensing).
  */
  licensing?: Licensing;
  /**
  * Provenance: who curated this collection, with what tool.
  */
  metadata?: AnnotationMetadata;
  /**
  * Human-readable name for this collection.
  */
  name: string;
  /**
  * Reference to a pub.layers.ontology defining the type system for entries in this collection.
  */
  ontologyRef?: string;
  /**
  * Version string (e.g., 'FrameNet 1.7', 'PropBank 3.4').
  */
  version?: string;
}

export type CollectionKind = "lexicon" | "frame-inventory" | "gazetteer" | "paradigm" | "stop-list" | "stimulus-pool" | "custom" | string & {};
