// @generated by idiolect-codegen. do not edit.
// source: pub.layers.acquisition.session

import type { AnnotationMetadata, EthicsApproval, FeatureMap, KnowledgeRef, LanguageRef, Licensing, ObjectRef, ReproducibilityInfo } from "./../defs";
import type { PresentationSpec } from "./../judgment/defs";
import type { DeviceInfo } from "./../media/defs";
import type { AccessCondition, Consent, Run, Stream } from "././defs";

// A synchronized recording occasion. Declares one time base shared by several media streams, so an annotation at 12340 ms resolves simultaneously against the lapel microphone, two camera angles, the EEG cap and the eye tracker.

/**
* One acquisition session. Corresponds to BIDS ses-<label>, a logical grouping of neuroimaging and behavioral data consistent across subjects, and to one NWB file, whose session_description, identifier and session_start_time are all required there and none of which has a home in Layers 0.8.0. Without this record a synchronized multi-stream recording is unrepresentable, and adding signal media alone would leave cross-stream annotation unanchorable.
*/
export interface Session {
  /**
  * Conditions under which this session's data may be obtained.
  */
  access?: AccessCondition;
  /**
  * What t=0 means (fallback when clockUri unavailable). Every stream offset and every anchor into this session is measured from it, so leaving it implicit would make every cross-stream anchor ambiguous.
  */
  clock?: SessionClock;
  /**
  * AT-URI of the session clock definition node.
  */
  clockUri?: string;
  /**
  * Session-level consent when it differs from the participants' standing consent. Absent means the participants' declarations govern.
  */
  consent?: Consent;
  /**
  * When this session record was created.
  */
  createdAt: string;
  /**
  * Instruments used, structured rather than a free-text device string; each a pub.layers.media.defs#deviceInfo.
  */
  devices?: DeviceInfo[];
  /**
  * Session duration in nanoseconds.
  */
  durationNanos?: number;
  /**
  * Recording environment slug (fallback when environmentUri unavailable). Determines what a consumer may assume about noise floor, lighting and electromagnetic interference.
  */
  environment?: SessionEnvironment;
  /**
  * AT-URI of the recording environment definition node.
  */
  environmentUri?: string;
  /**
  * AT-URIs of eprint records describing or associated with this session.
  */
  eprintRefs?: string[];
  /**
  * Human-subjects or animal-care approvals this session was conducted under, each a pub.layers.defs#ethicsApproval (the shared def, reused rather than a bespoke shape). BIDS EthicsApprovals is an optional array of free-text strings upstream.
  */
  ethicsApprovals?: EthicsApproval[];
  /**
  * AT-URI of the pub.layers.judgment.experimentDef this session instantiates. An experimentDef is a type-level protocol; a session is the token event that ran it, and 0.8.0 has only the type level.
  */
  experimentRef?: string;
  /**
  * Protocol deviations, timestamp coarsening, surgery, pharmacology, stimulus notes. NWB protocol, surgery, virus, pharmacology, data_collection and stimulus_notes land here until any earns a typed field.
  */
  features?: FeatureMap;
  /**
  * Knowledge graph references for this session (task grounded via cognitive-atlas, site via ror).
  */
  knowledgeRefs?: KnowledgeRef[];
  /**
  * Human-readable label. NWB session_description.
  */
  label?: string;
  /**
  * Structured language references complementing languages, each a pub.layers.defs#languageRef. No array-level cap; the record size limit governs.
  */
  languageRefs?: LanguageRef[];
  /**
  * Canonical BCP-47 tags this session's data covers, including sign language tags. No array-level cap; the record size limit governs.
  */
  languages?: string[];
  /**
  * Governs the artifact; consent governs the person; access governs the gate.
  */
  licensing?: Licensing;
  /**
  * The stream whose clock defines t=0, when clock is master-stream-start. An objectRef whose recordRef is this session and whose objectId is the stream uuid.
  */
  masterStream?: ObjectRef;
  /**
  * Provenance: who created this session record, with what tool.
  */
  metadata?: AnnotationMetadata;
  /**
  * AT-URIs of the pub.layers.acquisition.participant records recorded. An array because dialogue, interview, map-task and elicitation sessions have several, and each stream may belong to a different one.
  */
  participantRefs?: string[];
  /**
  * How stimuli were displayed, including screen geometry, which gaze-on-screen eye-tracking requires and which BIDS places in the events sidecar StimulusPresentation object rather than on the tracker.
  */
  presentation?: PresentationSpec;
  /**
  * How the acquisition was driven computationally, plus funding, via the shared reproducibilityInfo shape. Session-level ethics approvals are carried by ethicsApprovals. BIDS Funding is a free-text string array upstream.
  */
  reproducibility?: ReproducibilityInfo;
  /**
  * Sub-divisions of the session. BIDS run-<index>, an uninterrupted repetition of data acquisition with the same acquisition parameters and task. A session routinely spans several runs and, per BIDS Motion, several tracking systems at different sampling rates, which is why a session cannot carry one sampling rate.
  */
  runs?: Run[];
  /**
  * Stable session identifier within the authoring repo (ses-01, buckeye-s01-b03). BIDS session_id, REQUIRED in sessions.tsv; NWB session_id.
  */
  sessionId: string;
  /**
  * Institution or site, grounded via ror. Not an at-uri: an institution is an external entity Layers references, not a Layers record. NWB institution and lab.
  */
  siteRef?: KnowledgeRef;
  /**
  * Acquisition start. BIDS sessions.tsv and scans.tsv acq_time; NWB session_start_time, required there. MAY be coarsened for de-identification; when it is, say so in features, because a consumer cannot otherwise distinguish a coarsened timestamp from a precise one.
  */
  startedAt?: string;
  /**
  * The synchronized media streams making up this session. Inline rather than an edge record because a session is authored by one party who controls all its streams, the same reasoning that makes resource.templateComposition.members inline while corpus.membership is a record.
  */
  streams: Stream[];
  /**
  * Task slug (fallback when taskUri unavailable). BIDS TaskName is REQUIRED in every EEG, MEG, iEEG and NIRS sidecar, yet 0.8.0's only task construct is judgment.experimentDef, reachable only from judgmentSet.experimentRef, so a neural recording with no behavioural judgments could not name what the participant was doing.
  */
  task?: SessionTask;
  /**
  * AT-URI of the task definition node; ground to the Cognitive Atlas through knowledgeRefs where possible.
  */
  taskUri?: string;
}

export type SessionClock = "session-start" | "first-stream-start" | "master-stream-start" | "first-trigger" | "wall-clock" | "custom" | string & {};

export type SessionEnvironment = "sound-booth" | "laboratory" | "field" | "home" | "classroom" | "clinic" | "mri-scanner" | "meg-shielded-room" | "operating-room" | "online" | "custom" | string & {};

export type SessionTask = "rest" | "naturalistic-listening" | "naturalistic-viewing" | "reading" | "self-paced-reading" | "acceptability-judgment" | "picture-naming" | "lexical-decision" | "priming" | "visual-world" | "oddball" | "elicitation" | "free-conversation" | "interview" | "narrative" | "map-task" | "wordlist" | "repetition" | "production" | "custom" | string & {};
