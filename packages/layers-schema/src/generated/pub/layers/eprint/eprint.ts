// @generated by idiolect-codegen. do not edit.
// source: pub.layers.eprint.eprint

import type { FeatureMap, KnowledgeRef } from "./../defs";
import type { Citation } from "././defs";

/**
* A link between a Layers data record and an eprint.
*/
export interface Eprint {
  /**
  * References to specific annotation records linked to this eprint.
  */
  annotationRefs?: string[];
  /**
  * Bibliographic citation for the eprint. Carries rendered metadata (authors, title, venue, year) as a raw string and/or structured CSL-JSON/DataCite fields; eprintIdentifier/eprintIdentifierType remain the canonical link key.
  */
  citation?: Citation;
  /**
  * Reference to a corpus record.
  */
  corpusRef?: string;
  createdAt: string;
  /**
  * Description of the relationship.
  */
  description?: string;
  /**
  * The eprint identifier (DOI, arXiv ID, ACL Anthology ID, etc.).
  */
  eprintIdentifier: string;
  /**
  * Identifier type slug (fallback when eprintIdentifierTypeUri unavailable).
  */
  eprintIdentifierType?: EprintEprintIdentifierType;
  /**
  * AT-URI of the identifier type definition node. Community-expandable via knowledge graph.
  */
  eprintIdentifierTypeUri?: string;
  /**
  * Full URI of the eprint.
  */
  eprintUri?: string;
  /**
  * References to Layers expressions linked to this eprint.
  */
  expressionRefs?: string[];
  features?: FeatureMap;
  /**
  * Knowledge graph references (e.g., Wikidata for the venue, DBLP, Semantic Scholar corpus ID).
  */
  knowledgeRefs?: KnowledgeRef[];
  /**
  * Link type slug (fallback when linkTypeUri unavailable).
  */
  linkType: EprintLinkType;
  /**
  * AT-URI of the link type definition node. Community-expandable via knowledge graph.
  */
  linkTypeUri?: string;
  /**
  * AT-URIs of the eprint record on its publication platforms (e.g., chive.pub, any ATProto-native publication service). An eprint may be mirrored on multiple platforms.
  */
  platformEprintRefs?: string[];
}

export type EprintEprintIdentifierType = "doi" | "arxiv" | "acl-anthology" | "semantic-scholar" | "pubmed" | "isbn" | "url" | "at-uri" | "custom" | string & {};

export type EprintLinkType = "produced-by" | "described-in" | "evaluated-in" | "replicated-from" | "extends" | "supplements" | "cited-in" | "annotates" | "training-data-for" | "test-data-for" | string & {};
