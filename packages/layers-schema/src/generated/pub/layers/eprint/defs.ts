// @generated by idiolect-codegen. do not edit.
// source: pub.layers.eprint.defs

import type { AgentRef, KnowledgeRef } from "./../defs";

/**
* A bibliographic citation, expressible as a raw formatted string (raw) and/or structured fields following CSL-JSON and DataCite conventions. Consumers prefer the structured fields when present and fall back to raw. Populate at least raw or title.
*/
export interface Citation {
  /**
  * Abstract (CSL 'abstract'; DataCite Abstract description).
  */
  abstract?: string;
  /**
  * Date the work was accessed (CSL 'accessed').
  */
  accessed?: Date;
  /**
  * Title of the container: journal, book, or proceedings (CSL 'container-title').
  */
  containerTitle?: string;
  /**
  * Authors, editors, and other creators (CSL name variables; DataCite creators).
  */
  creators?: Creator[];
  /**
  * DOI (CSL 'DOI'), e.g., '10.18653/v1/2020.acl-main.1'.
  */
  doi?: string;
  edition?: string;
  isbn?: string;
  issn?: string;
  issue?: string;
  /**
  * Issue/publication date (CSL 'issued'; DataCite publicationYear).
  */
  issued?: Date;
  /**
  * External grounding for the work or venue (Crossref, OpenAlex, DBLP, Semantic Scholar).
  */
  knowledgeRefs?: KnowledgeRef[];
  /**
  * BCP-47 language tag of the work (CSL 'language').
  */
  language?: string;
  /**
  * Page range (CSL 'page', e.g., '101-126').
  */
  page?: string;
  /**
  * PubMed ID (CSL 'PMID').
  */
  pmid?: string;
  /**
  * Publisher (CSL 'publisher'; DataCite publisher).
  */
  publisher?: string;
  /**
  * Publisher location (CSL 'publisher-place').
  */
  publisherPlace?: string;
  /**
  * Full formatted citation string (CSL fallback). Sufficient on its own when structured metadata is unavailable.
  */
  raw?: string;
  /**
  * Title of the work (CSL 'title').
  */
  title?: string;
  /**
  * CSL-JSON item type slug (fallback when typeUri unavailable).
  */
  type?: CitationType;
  /**
  * AT-URI of the bibliographic type definition node. Community-expandable via knowledge graph.
  */
  typeUri?: string;
  /**
  * URL (CSL 'URL').
  */
  url?: string;
  /**
  * Version of the cited artifact (CSL 'version'; DataCite version).
  */
  version?: string;
  volume?: string;
}

/**
* A bibliographic creator (author, editor, etc.). Name parts follow CSL-JSON; nameType and affiliation follow DataCite. ATProto-native creators additionally ground identity via agent (DID) or knowledgeRef (ORCID, ROR, OpenAlex).
*/
export interface Creator {
  /**
  * Affiliation (DataCite affiliation).
  */
  affiliation?: string;
  /**
  * ATProto-native identity grounding (DID) for the creator, if any.
  */
  agent?: AgentRef;
  /**
  * Dropping particle (CSL 'dropping-particle').
  */
  droppingParticle?: string;
  /**
  * Family/surname (CSL 'family').
  */
  family?: string;
  /**
  * Given name(s) (CSL 'given').
  */
  given?: string;
  /**
  * External identity grounding (ORCID via source 'orcid', ROR for organizations, OpenAlex author id).
  */
  knowledgeRef?: KnowledgeRef;
  /**
  * Full name as a single string when parts are not separated, or an organization name (CSL 'literal').
  */
  literal?: string;
  /**
  * Whether the creator is a person or an organization (DataCite nameType; fallback when nameTypeUri unavailable).
  */
  nameType?: CreatorNameType;
  /**
  * AT-URI of the name-type definition node. Community-expandable via knowledge graph.
  */
  nameTypeUri?: string;
  /**
  * Non-dropping particle (CSL 'non-dropping-particle').
  */
  nonDroppingParticle?: string;
  /**
  * Creator role slug (CSL contributor role / DataCite contributorType; fallback when roleUri unavailable).
  */
  role?: CreatorRole;
  /**
  * AT-URI of the creator-role definition node. Community-expandable via knowledge graph.
  */
  roleUri?: string;
  /**
  * Position of this creator in the creator list (1-based).
  */
  sequence?: number;
  /**
  * Name suffix (CSL 'suffix', e.g., 'Jr.', 'III').
  */
  suffix?: string;
}

/**
* A date in CSL-JSON style: structured year/month/day and/or a free-form literal. Maps to CSL 'date-parts' (when year/month/day are set) or 'literal'/'raw' (when only literal is set).
*/
export interface Date {
  /**
  * Whether the date is approximate (CSL 'circa').
  */
  circa?: boolean;
  /**
  * Day of month (1-31).
  */
  day?: number;
  /**
  * Free-form date string when structured parts are unavailable (CSL 'literal'/'raw').
  */
  literal?: string;
  /**
  * Month (1-12).
  */
  month?: number;
  /**
  * Season when the work was issued (CSL 'season').
  */
  season?: string;
  /**
  * Four-digit year.
  */
  year?: number;
}

export type CitationType = "article-journal" | "paper-conference" | "article" | "book" | "chapter" | "thesis" | "report" | "dataset" | "software" | "manuscript" | "webpage" | "entry" | "standard" | "preprint" | "custom" | string & {};

export type CreatorNameType = "personal" | "organizational" | string & {};

export type CreatorRole = "author" | "editor" | "translator" | "contributor" | "illustrator" | "director" | "producer" | "collector" | "custom" | string & {};
