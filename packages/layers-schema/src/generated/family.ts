// @generated by idiolect-codegen. do not edit.

// Generated record family for `pub.layers`.
//
// Per-record types come from the sibling generated modules. This file
// emits the discriminated-union view, the dispatch helpers, and the
// family identity (`FAMILY_ID`, `FAMILY_NSID_PREFIX`, `FamilyMarker`)
// that mirror the Rust `family.rs` surface.

import type { Alignment } from "./pub/layers/alignment/alignment";
import type { AnnotationLayer } from "./pub/layers/annotation/annotation_layer";
import type { ClusterSet } from "./pub/layers/annotation/cluster_set";
import type { Entry as ChangelogEntry } from "./pub/layers/changelog/entry";
import type { Corpus } from "./pub/layers/corpus/corpus";
import type { Membership } from "./pub/layers/corpus/membership";
import type { DataLink } from "./pub/layers/eprint/data_link";
import type { Eprint } from "./pub/layers/eprint/eprint";
import type { Expression } from "./pub/layers/expression/expression";
import type { GraphEdge } from "./pub/layers/graph/graph_edge";
import type { GraphEdgeSet } from "./pub/layers/graph/graph_edge_set";
import type { GraphNode } from "./pub/layers/graph/graph_node";
import type { AgreementReport } from "./pub/layers/judgment/agreement_report";
import type { ExperimentDef } from "./pub/layers/judgment/experiment_def";
import type { JudgmentSet } from "./pub/layers/judgment/judgment_set";
import type { Media } from "./pub/layers/media/media";
import type { Ontology } from "./pub/layers/ontology/ontology";
import type { TypeDef } from "./pub/layers/ontology/type_def";
import type { Persona } from "./pub/layers/persona/persona";
import type { Collection } from "./pub/layers/resource/collection";
import type { CollectionMembership } from "./pub/layers/resource/collection_membership";
import type { Entry as ResourceEntry } from "./pub/layers/resource/entry";
import type { Filling } from "./pub/layers/resource/filling";
import type { Template } from "./pub/layers/resource/template";
import type { TemplateComposition } from "./pub/layers/resource/template_composition";
import type { Segmentation } from "./pub/layers/segmentation/segmentation";

/** Family identifier, mirrored from the Rust `RecordFamily::ID` constant. */
export const FAMILY_ID = "pub.layers" as const;

/** NSID prefix every member of this family shares. Informational. */
export const FAMILY_NSID_PREFIX = "pub.layers." as const;

/** Nominal marker for the family, mirrored from the Rust `LayersFamily` struct. */
export type FamilyMarker = "LayersFamily";

/**
 * Canonical NSIDs, keyed by record kind for ergonomic call sites.
 */
export const NSID = {
  alignment: "pub.layers.alignment.alignment",
  annotation_layer: "pub.layers.annotation.annotationLayer",
  cluster_set: "pub.layers.annotation.clusterSet",
  changelogEntry: "pub.layers.changelog.entry",
  corpus: "pub.layers.corpus.corpus",
  membership: "pub.layers.corpus.membership",
  data_link: "pub.layers.eprint.dataLink",
  eprint: "pub.layers.eprint.eprint",
  expression: "pub.layers.expression.expression",
  graph_edge: "pub.layers.graph.graphEdge",
  graph_edge_set: "pub.layers.graph.graphEdgeSet",
  graph_node: "pub.layers.graph.graphNode",
  agreement_report: "pub.layers.judgment.agreementReport",
  experiment_def: "pub.layers.judgment.experimentDef",
  judgment_set: "pub.layers.judgment.judgmentSet",
  media: "pub.layers.media.media",
  ontology: "pub.layers.ontology.ontology",
  type_def: "pub.layers.ontology.typeDef",
  persona: "pub.layers.persona.persona",
  collection: "pub.layers.resource.collection",
  collection_membership: "pub.layers.resource.collectionMembership",
  resourceEntry: "pub.layers.resource.entry",
  filling: "pub.layers.resource.filling",
  template: "pub.layers.resource.template",
  template_composition: "pub.layers.resource.templateComposition",
  segmentation: "pub.layers.segmentation.segmentation",
} as const;

export type NSID = (typeof NSID)[keyof typeof NSID];

/**
 * Mapping from record NSID to its TypeScript record type.
 */
export type RecordTypes = {
  [NSID.alignment]: Alignment;
  [NSID.annotation_layer]: AnnotationLayer;
  [NSID.cluster_set]: ClusterSet;
  [NSID.changelogEntry]: ChangelogEntry;
  [NSID.corpus]: Corpus;
  [NSID.membership]: Membership;
  [NSID.data_link]: DataLink;
  [NSID.eprint]: Eprint;
  [NSID.expression]: Expression;
  [NSID.graph_edge]: GraphEdge;
  [NSID.graph_edge_set]: GraphEdgeSet;
  [NSID.graph_node]: GraphNode;
  [NSID.agreement_report]: AgreementReport;
  [NSID.experiment_def]: ExperimentDef;
  [NSID.judgment_set]: JudgmentSet;
  [NSID.media]: Media;
  [NSID.ontology]: Ontology;
  [NSID.type_def]: TypeDef;
  [NSID.persona]: Persona;
  [NSID.collection]: Collection;
  [NSID.collection_membership]: CollectionMembership;
  [NSID.resourceEntry]: ResourceEntry;
  [NSID.filling]: Filling;
  [NSID.template]: Template;
  [NSID.template_composition]: TemplateComposition;
  [NSID.segmentation]: Segmentation;
};

/**
 * Discriminated union tagged by `$nsid` for runtime dispatch.
 */
export type AnyRecord =
  | { readonly $nsid: typeof NSID.alignment; readonly value: Alignment }
  | { readonly $nsid: typeof NSID.annotation_layer; readonly value: AnnotationLayer }
  | { readonly $nsid: typeof NSID.cluster_set; readonly value: ClusterSet }
  | { readonly $nsid: typeof NSID.changelogEntry; readonly value: ChangelogEntry }
  | { readonly $nsid: typeof NSID.corpus; readonly value: Corpus }
  | { readonly $nsid: typeof NSID.membership; readonly value: Membership }
  | { readonly $nsid: typeof NSID.data_link; readonly value: DataLink }
  | { readonly $nsid: typeof NSID.eprint; readonly value: Eprint }
  | { readonly $nsid: typeof NSID.expression; readonly value: Expression }
  | { readonly $nsid: typeof NSID.graph_edge; readonly value: GraphEdge }
  | { readonly $nsid: typeof NSID.graph_edge_set; readonly value: GraphEdgeSet }
  | { readonly $nsid: typeof NSID.graph_node; readonly value: GraphNode }
  | { readonly $nsid: typeof NSID.agreement_report; readonly value: AgreementReport }
  | { readonly $nsid: typeof NSID.experiment_def; readonly value: ExperimentDef }
  | { readonly $nsid: typeof NSID.judgment_set; readonly value: JudgmentSet }
  | { readonly $nsid: typeof NSID.media; readonly value: Media }
  | { readonly $nsid: typeof NSID.ontology; readonly value: Ontology }
  | { readonly $nsid: typeof NSID.type_def; readonly value: TypeDef }
  | { readonly $nsid: typeof NSID.persona; readonly value: Persona }
  | { readonly $nsid: typeof NSID.collection; readonly value: Collection }
  | { readonly $nsid: typeof NSID.collection_membership; readonly value: CollectionMembership }
  | { readonly $nsid: typeof NSID.resourceEntry; readonly value: ResourceEntry }
  | { readonly $nsid: typeof NSID.filling; readonly value: Filling }
  | { readonly $nsid: typeof NSID.template; readonly value: Template }
  | { readonly $nsid: typeof NSID.template_composition; readonly value: TemplateComposition }
  | { readonly $nsid: typeof NSID.segmentation; readonly value: Segmentation };

/** True if `r` is an `AnyRecord` tagged with the given nsid. */
export function isKind<K extends NSID>(
  r: AnyRecord,
  nsid: K,
): r is Extract<AnyRecord, { $nsid: K }> {
  return r.$nsid === nsid;
}

/** True if `r` wraps a `Alignment`. */
export function isAlignment(r: AnyRecord): r is { readonly $nsid: typeof NSID.alignment; readonly value: Alignment } {
  return r.$nsid === NSID.alignment;
}

/** True if `r` wraps a `AnnotationLayer`. */
export function isAnnotationLayer(r: AnyRecord): r is { readonly $nsid: typeof NSID.annotation_layer; readonly value: AnnotationLayer } {
  return r.$nsid === NSID.annotation_layer;
}

/** True if `r` wraps a `ClusterSet`. */
export function isClusterSet(r: AnyRecord): r is { readonly $nsid: typeof NSID.cluster_set; readonly value: ClusterSet } {
  return r.$nsid === NSID.cluster_set;
}

/** True if `r` wraps a `ChangelogEntry`. */
export function isChangelogEntry(r: AnyRecord): r is { readonly $nsid: typeof NSID.changelogEntry; readonly value: ChangelogEntry } {
  return r.$nsid === NSID.changelogEntry;
}

/** True if `r` wraps a `Corpus`. */
export function isCorpus(r: AnyRecord): r is { readonly $nsid: typeof NSID.corpus; readonly value: Corpus } {
  return r.$nsid === NSID.corpus;
}

/** True if `r` wraps a `Membership`. */
export function isMembership(r: AnyRecord): r is { readonly $nsid: typeof NSID.membership; readonly value: Membership } {
  return r.$nsid === NSID.membership;
}

/** True if `r` wraps a `DataLink`. */
export function isDataLink(r: AnyRecord): r is { readonly $nsid: typeof NSID.data_link; readonly value: DataLink } {
  return r.$nsid === NSID.data_link;
}

/** True if `r` wraps a `Eprint`. */
export function isEprint(r: AnyRecord): r is { readonly $nsid: typeof NSID.eprint; readonly value: Eprint } {
  return r.$nsid === NSID.eprint;
}

/** True if `r` wraps a `Expression`. */
export function isExpression(r: AnyRecord): r is { readonly $nsid: typeof NSID.expression; readonly value: Expression } {
  return r.$nsid === NSID.expression;
}

/** True if `r` wraps a `GraphEdge`. */
export function isGraphEdge(r: AnyRecord): r is { readonly $nsid: typeof NSID.graph_edge; readonly value: GraphEdge } {
  return r.$nsid === NSID.graph_edge;
}

/** True if `r` wraps a `GraphEdgeSet`. */
export function isGraphEdgeSet(r: AnyRecord): r is { readonly $nsid: typeof NSID.graph_edge_set; readonly value: GraphEdgeSet } {
  return r.$nsid === NSID.graph_edge_set;
}

/** True if `r` wraps a `GraphNode`. */
export function isGraphNode(r: AnyRecord): r is { readonly $nsid: typeof NSID.graph_node; readonly value: GraphNode } {
  return r.$nsid === NSID.graph_node;
}

/** True if `r` wraps a `AgreementReport`. */
export function isAgreementReport(r: AnyRecord): r is { readonly $nsid: typeof NSID.agreement_report; readonly value: AgreementReport } {
  return r.$nsid === NSID.agreement_report;
}

/** True if `r` wraps a `ExperimentDef`. */
export function isExperimentDef(r: AnyRecord): r is { readonly $nsid: typeof NSID.experiment_def; readonly value: ExperimentDef } {
  return r.$nsid === NSID.experiment_def;
}

/** True if `r` wraps a `JudgmentSet`. */
export function isJudgmentSet(r: AnyRecord): r is { readonly $nsid: typeof NSID.judgment_set; readonly value: JudgmentSet } {
  return r.$nsid === NSID.judgment_set;
}

/** True if `r` wraps a `Media`. */
export function isMedia(r: AnyRecord): r is { readonly $nsid: typeof NSID.media; readonly value: Media } {
  return r.$nsid === NSID.media;
}

/** True if `r` wraps a `Ontology`. */
export function isOntology(r: AnyRecord): r is { readonly $nsid: typeof NSID.ontology; readonly value: Ontology } {
  return r.$nsid === NSID.ontology;
}

/** True if `r` wraps a `TypeDef`. */
export function isTypeDef(r: AnyRecord): r is { readonly $nsid: typeof NSID.type_def; readonly value: TypeDef } {
  return r.$nsid === NSID.type_def;
}

/** True if `r` wraps a `Persona`. */
export function isPersona(r: AnyRecord): r is { readonly $nsid: typeof NSID.persona; readonly value: Persona } {
  return r.$nsid === NSID.persona;
}

/** True if `r` wraps a `Collection`. */
export function isCollection(r: AnyRecord): r is { readonly $nsid: typeof NSID.collection; readonly value: Collection } {
  return r.$nsid === NSID.collection;
}

/** True if `r` wraps a `CollectionMembership`. */
export function isCollectionMembership(r: AnyRecord): r is { readonly $nsid: typeof NSID.collection_membership; readonly value: CollectionMembership } {
  return r.$nsid === NSID.collection_membership;
}

/** True if `r` wraps a `ResourceEntry`. */
export function isResourceEntry(r: AnyRecord): r is { readonly $nsid: typeof NSID.resourceEntry; readonly value: ResourceEntry } {
  return r.$nsid === NSID.resourceEntry;
}

/** True if `r` wraps a `Filling`. */
export function isFilling(r: AnyRecord): r is { readonly $nsid: typeof NSID.filling; readonly value: Filling } {
  return r.$nsid === NSID.filling;
}

/** True if `r` wraps a `Template`. */
export function isTemplate(r: AnyRecord): r is { readonly $nsid: typeof NSID.template; readonly value: Template } {
  return r.$nsid === NSID.template;
}

/** True if `r` wraps a `TemplateComposition`. */
export function isTemplateComposition(r: AnyRecord): r is { readonly $nsid: typeof NSID.template_composition; readonly value: TemplateComposition } {
  return r.$nsid === NSID.template_composition;
}

/** True if `r` wraps a `Segmentation`. */
export function isSegmentation(r: AnyRecord): r is { readonly $nsid: typeof NSID.segmentation; readonly value: Segmentation } {
  return r.$nsid === NSID.segmentation;
}

/**
 * Wrap a strongly-typed record in its `AnyRecord` variant.
 */
export function tagRecord<K extends NSID>(
  nsid: K,
  value: RecordTypes[K],
): AnyRecord {
  return { $nsid: nsid, value } as AnyRecord;
}

/** All record NSIDs in declaration order. */
export const RECORD_NSIDS = [
  NSID.alignment,
  NSID.annotation_layer,
  NSID.cluster_set,
  NSID.changelogEntry,
  NSID.corpus,
  NSID.membership,
  NSID.data_link,
  NSID.eprint,
  NSID.expression,
  NSID.graph_edge,
  NSID.graph_edge_set,
  NSID.graph_node,
  NSID.agreement_report,
  NSID.experiment_def,
  NSID.judgment_set,
  NSID.media,
  NSID.ontology,
  NSID.type_def,
  NSID.persona,
  NSID.collection,
  NSID.collection_membership,
  NSID.resourceEntry,
  NSID.filling,
  NSID.template,
  NSID.template_composition,
  NSID.segmentation,
] as const satisfies readonly NSID[];

const FAMILY_NSID_SET: ReadonlySet<string> = new Set(RECORD_NSIDS);

/**
 * True if `nsid` is a member of this family — exact match against
 * the family's record set. Mirrors the Rust `RecordFamily::contains`
 * predicate; the type narrowing to `NSID` is sound because the
 * runtime check tests against the same literal set the type encodes.
 */
export function familyContains(nsid: string): nsid is NSID {
  return FAMILY_NSID_SET.has(nsid);
}

/**
 * Loose decoded view: family NSID and an unvalidated record body.
 * `decodeRecord` produces this; callers pair it with a per-record
 * validator (Zod, io-ts, hand-rolled) before treating the body as
 * any specific record type.
 */
export interface DecodedRecord {
  readonly $nsid: NSID;
  readonly body: unknown;
}

/**
 * Split an atproto wire-form record (an object whose `$type` field
 * carries the NSID of the contained record) into a (`$nsid`, body)
 * pair. Mirrors the Rust `AnyRecord::from_typed_json` constructor in
 * shape, but TypeScript has no runtime structural validator for the
 * generated record types, so the body comes back as `unknown` and
 * the caller is responsible for narrowing it.
 *
 * Returns `null` if `value` is not structurally a record object or
 * its `$type` is outside this family.
 */
export function decodeRecord(value: unknown): DecodedRecord | null {
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    return null;
  }
  const obj = value as Record<string, unknown>;
  const ty = obj["$type"];
  if (typeof ty !== "string" || !familyContains(ty)) return null;
  const { $type: _stripped, ...body } = obj;
  return { $nsid: ty, body };
}

/**
 * Encode an `AnyRecord` into atproto wire form: the inner `value`
 * spread with a `$type` discriminator. Mirrors the Rust
 * `AnyRecord::to_typed_json` method.
 */
export function toTypedJson(r: AnyRecord): Record<string, unknown> {
  return { ...r.value, $type: r.$nsid } as Record<string, unknown>;
}
