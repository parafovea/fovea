{
  "lexicon": 1,
  "id": "pub.layers.changelog.getEntry",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single changelog entry by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the changelog entry record to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned changelog entry record." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned changelog entry record at this revision." },
            "value": { "type": "ref", "ref": "pub.layers.changelog.entry#main", "description": "The changelog entry record value." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
