{
  "lexicon": 1,
  "id": "pub.layers.annotation.listClusterSets",
  "defs": {
    "main": {
      "type": "query",
      "description": "List cluster sets.",
      "parameters": {
        "type": "params",
        "required": ["expression"],
        "properties": {
          "expression": { "type": "string", "format": "at-uri", "description": "AT-URI of the expression whose cluster sets to list." },
          "kind": { "type": "string", "description": "Filter to cluster sets of this clustering kind slug (e.g., coreference, bridging)." },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 50, "description": "Maximum number of cluster sets to return in one page." },
          "cursor": { "type": "string", "description": "Opaque pagination cursor to resume from a previous page." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["records"],
          "properties": {
            "cursor": { "type": "string", "description": "Opaque pagination cursor for the next page, absent when no further results remain." },
            "records": {
              "type": "array",
              "description": "The page of cluster set records returned.",
              "items": { "type": "ref", "ref": "#recordView" }
            }
          }
        }
      }
    },
    "recordView": {
      "type": "object",
      "required": ["uri", "cid", "value"],
      "properties": {
        "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of this cluster set record." },
        "cid": { "type": "string", "format": "cid", "description": "CID of this cluster set record at this revision." },
        "value": { "type": "ref", "ref": "pub.layers.annotation.clusterSet#main", "description": "The cluster set record value." }
      }
    }
  }
}
