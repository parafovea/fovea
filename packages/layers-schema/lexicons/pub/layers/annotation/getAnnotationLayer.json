{
  "lexicon": 1,
  "id": "pub.layers.annotation.getAnnotationLayer",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single annotation layer by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the annotation layer record to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned annotation layer record." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned annotation layer record at this revision." },
            "value": { "type": "ref", "ref": "pub.layers.annotation.annotationLayer#main", "description": "The annotation layer record value." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
