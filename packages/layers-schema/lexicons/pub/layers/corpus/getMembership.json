{
  "lexicon": 1,
  "id": "pub.layers.corpus.getMembership",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single membership by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the membership record to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned membership record." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned membership record at this revision." },
            "value": { "type": "ref", "ref": "pub.layers.corpus.membership#main", "description": "The membership record value." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
