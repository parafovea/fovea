{
  "lexicon": 1,
  "id": "pub.layers.corpus.getCorpus",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single corpus by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the corpus record to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned corpus record." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned corpus record at this revision." },
            "value": { "type": "ref", "ref": "pub.layers.corpus.corpus#main", "description": "The corpus record value." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
