{
  "lexicon": 1,
  "id": "pub.layers.alignment.getAlignment",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single alignment by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the alignment record to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned alignment record." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned alignment record at this revision." },
            "value": { "type": "ref", "ref": "pub.layers.alignment.alignment#main", "description": "The alignment record value." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
