{
  "lexicon": 1,
  "id": "pub.layers.expression.getExpression",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single expression by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the expression record to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned expression record." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned expression record at this revision." },
            "value": { "type": "ref", "ref": "pub.layers.expression.expression#main", "description": "The expression record value." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
