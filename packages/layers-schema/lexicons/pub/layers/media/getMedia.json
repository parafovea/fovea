{
  "lexicon": 1,
  "id": "pub.layers.media.getMedia",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single media record by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the media record to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned media record." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned media record at this revision." },
            "value": { "type": "ref", "ref": "pub.layers.media.media#main", "description": "The media record value." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
