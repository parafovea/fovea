{
  "lexicon": 1,
  "id": "pub.layers.acquisition.getParticipant",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single participant record by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the participant record to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned participant record." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned participant record revision." },
            "value": { "type": "ref", "ref": "pub.layers.acquisition.participant#main", "description": "The participant record value." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
