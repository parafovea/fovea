{
  "lexicon": 1,
  "id": "pub.layers.graph.listGraphEdgeSets",
  "defs": {
    "main": {
      "type": "query",
      "description": "List graph edge sets.",
      "parameters": {
        "type": "params",
        "required": ["repo"],
        "properties": {
          "repo": { "type": "string", "format": "at-identifier", "description": "The repository (DID or handle) whose graph edge sets to list." },
          "edgeType": { "type": "string", "description": "Filter to edge sets with this edge type slug." },
          "expression": { "type": "string", "format": "at-uri", "description": "Filter to edge sets scoped to this expression AT-URI." },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 50, "description": "Maximum number of records to return in one page." },
          "cursor": { "type": "string", "description": "Opaque pagination cursor from a previous response, to resume after the last returned record." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["records"],
          "properties": {
            "cursor": { "type": "string", "description": "Opaque pagination cursor to pass to the next call, absent when no further pages remain." },
            "records": {
              "type": "array",
              "description": "The page of graph edge set records returned.",
              "items": { "type": "ref", "ref": "#recordView" }
            }
          }
        }
      }
    },
    "recordView": {
      "type": "object",
      "required": ["uri", "cid", "value"],
      "properties": {
        "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of this graph edge set record." },
        "cid": { "type": "string", "format": "cid", "description": "CID of this graph edge set record's indexed revision." },
        "value": { "type": "ref", "ref": "pub.layers.graph.graphEdgeSet#main", "description": "The graph edge set record value." }
      }
    }
  }
}
