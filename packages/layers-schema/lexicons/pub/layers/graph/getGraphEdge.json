{
  "lexicon": 1,
  "id": "pub.layers.graph.getGraphEdge",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single graph edge by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the graph edge record to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned graph edge record." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned graph edge record at this revision." },
            "value": { "type": "ref", "ref": "pub.layers.graph.graphEdge#main", "description": "The graph edge record value." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
