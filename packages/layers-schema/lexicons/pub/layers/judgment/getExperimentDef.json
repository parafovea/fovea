{
  "lexicon": 1,
  "id": "pub.layers.judgment.getExperimentDef",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single experiment definition by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the experiment definition record to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned experiment definition record." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned experiment definition record revision." },
            "value": { "type": "ref", "ref": "pub.layers.judgment.experimentDef#main", "description": "The experiment definition record value." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
