{
  "lexicon": 1,
  "id": "pub.layers.judgment.listExperimentDefs",
  "defs": {
    "main": {
      "type": "query",
      "description": "List experiment definitions.",
      "parameters": {
        "type": "params",
        "required": ["repo"],
        "properties": {
          "repo": { "type": "string", "format": "at-identifier", "description": "The repository (DID or handle) whose experiment definitions to list." },
          "measureType": { "type": "string", "description": "Filter to experiment definitions with this measure type slug." },
          "taskType": { "type": "string", "description": "Filter to experiment definitions with this response instrument (task type) slug." },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 50, "description": "Maximum number of records to return in one page." },
          "cursor": { "type": "string", "description": "Opaque pagination cursor from a previous response, to resume after the last returned record." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["records"],
          "properties": {
            "cursor": { "type": "string", "description": "Opaque pagination cursor to pass to the next call, absent when no further pages remain." },
            "records": {
              "type": "array",
              "description": "The page of experiment definition records returned.",
              "items": { "type": "ref", "ref": "#recordView" }
            }
          }
        }
      }
    },
    "recordView": {
      "type": "object",
      "required": ["uri", "cid", "value"],
      "properties": {
        "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of this experiment definition record." },
        "cid": { "type": "string", "format": "cid", "description": "CID of this experiment definition record's indexed revision." },
        "value": { "type": "ref", "ref": "pub.layers.judgment.experimentDef#main", "description": "The experiment definition record value." }
      }
    }
  }
}
