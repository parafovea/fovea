{
  "lexicon": 1,
  "id": "pub.layers.judgment.getAgreementReport",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single agreement report by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the agreement report record to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned agreement report record." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned agreement report record revision." },
            "value": { "type": "ref", "ref": "pub.layers.judgment.agreementReport#main", "description": "The agreement report record value." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
