{
  "lexicon": 1,
  "id": "pub.layers.judgment.listAgreementReports",
  "defs": {
    "main": {
      "type": "query",
      "description": "List agreement reports.",
      "parameters": {
        "type": "params",
        "required": ["experimentRef"],
        "properties": {
          "experimentRef": { "type": "string", "format": "at-uri", "description": "AT-URI of the experiment definition whose agreement reports to list." },
          "metric": { "type": "string", "description": "Filter to agreement reports computed with this metric slug." },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 50, "description": "Maximum number of records to return in one page." },
          "cursor": { "type": "string", "description": "Opaque pagination cursor from a previous response, to resume after the last returned record." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["records"],
          "properties": {
            "cursor": { "type": "string", "description": "Opaque pagination cursor to pass to the next call, absent when no further pages remain." },
            "records": {
              "type": "array",
              "description": "The page of agreement report records returned.",
              "items": { "type": "ref", "ref": "#recordView" }
            }
          }
        }
      }
    },
    "recordView": {
      "type": "object",
      "required": ["uri", "cid", "value"],
      "properties": {
        "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of this agreement report record." },
        "cid": { "type": "string", "format": "cid", "description": "CID of this agreement report record's indexed revision." },
        "value": { "type": "ref", "ref": "pub.layers.judgment.agreementReport#main", "description": "The agreement report record value." }
      }
    }
  }
}
