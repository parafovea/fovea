{
  "lexicon": 1,
  "id": "pub.layers.judgment.listJudgmentSets",
  "defs": {
    "main": {
      "type": "query",
      "description": "List judgment sets.",
      "parameters": {
        "type": "params",
        "required": ["experimentRef"],
        "properties": {
          "experimentRef": { "type": "string", "format": "at-uri", "description": "AT-URI of the experiment definition whose judgment sets to list." },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 50, "description": "Maximum number of records to return in one page." },
          "cursor": { "type": "string", "description": "Opaque pagination cursor from a previous response, to resume after the last returned record." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["records"],
          "properties": {
            "cursor": { "type": "string", "description": "Opaque pagination cursor to pass to the next call, absent when no further pages remain." },
            "records": {
              "type": "array",
              "description": "The page of judgment set records returned.",
              "items": { "type": "ref", "ref": "#recordView" }
            }
          }
        }
      }
    },
    "recordView": {
      "type": "object",
      "required": ["uri", "cid", "value"],
      "properties": {
        "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of this judgment set record." },
        "cid": { "type": "string", "format": "cid", "description": "CID of this judgment set record's indexed revision." },
        "value": { "type": "ref", "ref": "pub.layers.judgment.judgmentSet#main", "description": "The judgment set record value." }
      }
    }
  }
}
