{
  "lexicon": 1,
  "id": "pub.layers.ontology.getTypeDef",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single type definition by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the type definition to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned type definition." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned type definition's current revision." },
            "value": { "type": "ref", "ref": "pub.layers.ontology.typeDef#main", "description": "The type definition record itself." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
