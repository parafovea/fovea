{
  "lexicon": 1,
  "id": "pub.layers.eprint.listDataLinks",
  "defs": {
    "main": {
      "type": "query",
      "description": "List data links.",
      "parameters": {
        "type": "params",
        "required": ["eprintUri"],
        "properties": {
          "eprintUri": { "type": "string", "format": "at-uri", "description": "AT-URI of the eprint whose data links to list." },
          "dataKind": { "type": "string", "description": "Filter to data links with this data kind slug." },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 50, "description": "Maximum number of records to return in one page." },
          "cursor": { "type": "string", "description": "Opaque pagination cursor from a previous response, to resume after the last returned record." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["records"],
          "properties": {
            "cursor": { "type": "string", "description": "Opaque pagination cursor to pass to the next call, absent when no further pages remain." },
            "records": {
              "type": "array",
              "description": "The page of data link records returned.",
              "items": { "type": "ref", "ref": "#recordView" }
            }
          }
        }
      }
    },
    "recordView": {
      "type": "object",
      "required": ["uri", "cid", "value"],
      "properties": {
        "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of this data link record." },
        "cid": { "type": "string", "format": "cid", "description": "CID of this data link record's indexed revision." },
        "value": { "type": "ref", "ref": "pub.layers.eprint.dataLink#main", "description": "The data link record value." }
      }
    }
  }
}
