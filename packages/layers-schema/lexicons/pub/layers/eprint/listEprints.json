{
  "lexicon": 1,
  "id": "pub.layers.eprint.listEprints",
  "defs": {
    "main": {
      "type": "query",
      "description": "List eprints.",
      "parameters": {
        "type": "params",
        "required": ["repo"],
        "properties": {
          "repo": { "type": "string", "format": "at-identifier", "description": "The repository (DID or handle) whose eprints to list." },
          "linkType": { "type": "string", "description": "Filter to eprint links with this link type slug." },
          "eprintIdentifierType": { "type": "string", "description": "Filter to eprints with this identifier type slug." },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 50, "description": "Maximum number of records to return in one page." },
          "cursor": { "type": "string", "description": "Opaque pagination cursor from a previous response, to resume after the last returned record." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["records"],
          "properties": {
            "cursor": { "type": "string", "description": "Opaque pagination cursor to pass to the next call, absent when no further pages remain." },
            "records": {
              "type": "array",
              "description": "The page of eprint records returned.",
              "items": { "type": "ref", "ref": "#recordView" }
            }
          }
        }
      }
    },
    "recordView": {
      "type": "object",
      "required": ["uri", "cid", "value"],
      "properties": {
        "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of this eprint record." },
        "cid": { "type": "string", "format": "cid", "description": "CID of this eprint record's indexed revision." },
        "value": { "type": "ref", "ref": "pub.layers.eprint.eprint#main", "description": "The eprint record value." }
      }
    }
  }
}
