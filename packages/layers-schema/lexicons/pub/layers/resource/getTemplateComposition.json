{
  "lexicon": 1,
  "id": "pub.layers.resource.getTemplateComposition",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a single template composition by its AT-URI.",
      "parameters": {
        "type": "params",
        "required": ["uri"],
        "properties": {
          "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the template composition record to fetch." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["uri", "cid", "value"],
          "properties": {
            "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of the returned template composition record." },
            "cid": { "type": "string", "format": "cid", "description": "CID of the returned template composition record at this revision." },
            "value": { "type": "ref", "ref": "pub.layers.resource.templateComposition#main", "description": "The template composition record value." }
          }
        }
      },
      "errors": [{ "name": "RecordNotFound" }]
    }
  }
}
