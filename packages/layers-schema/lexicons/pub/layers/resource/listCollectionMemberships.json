{
  "lexicon": 1,
  "id": "pub.layers.resource.listCollectionMemberships",
  "defs": {
    "main": {
      "type": "query",
      "description": "List collection memberships.",
      "parameters": {
        "type": "params",
        "required": ["collectionRef"],
        "properties": {
          "collectionRef": { "type": "string", "format": "at-uri", "description": "AT-URI of the collection whose memberships to list." },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 50, "description": "Maximum number of records to return in one page." },
          "cursor": { "type": "string", "description": "Opaque pagination cursor from a previous response, to resume after the last returned record." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["records"],
          "properties": {
            "cursor": { "type": "string", "description": "Opaque pagination cursor to pass to the next call, absent when no further pages remain." },
            "records": {
              "type": "array",
              "description": "The page of collection membership records returned.",
              "items": { "type": "ref", "ref": "#recordView" }
            }
          }
        }
      }
    },
    "recordView": {
      "type": "object",
      "required": ["uri", "cid", "value"],
      "properties": {
        "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of this collection membership record." },
        "cid": { "type": "string", "format": "cid", "description": "CID of this collection membership record's indexed revision." },
        "value": { "type": "ref", "ref": "pub.layers.resource.collectionMembership#main", "description": "The collection membership record value." }
      }
    }
  }
}
