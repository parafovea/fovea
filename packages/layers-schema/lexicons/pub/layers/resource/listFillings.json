{
  "lexicon": 1,
  "id": "pub.layers.resource.listFillings",
  "defs": {
    "main": {
      "type": "query",
      "description": "List template fillings.",
      "parameters": {
        "type": "params",
        "required": ["templateRef"],
        "properties": {
          "templateRef": { "type": "string", "format": "at-uri", "description": "AT-URI of the template whose fillings to list." },
          "strategy": { "type": "string", "description": "Filter to fillings produced by this filling strategy slug." },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 50, "description": "Maximum number of records to return in one page." },
          "cursor": { "type": "string", "description": "Opaque pagination cursor from a previous response, to resume after the last returned record." }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["records"],
          "properties": {
            "cursor": { "type": "string", "description": "Opaque pagination cursor to pass to the next call, absent when no further pages remain." },
            "records": {
              "type": "array",
              "description": "The page of template filling records returned.",
              "items": { "type": "ref", "ref": "#recordView" }
            }
          }
        }
      }
    },
    "recordView": {
      "type": "object",
      "required": ["uri", "cid", "value"],
      "properties": {
        "uri": { "type": "string", "format": "at-uri", "description": "AT-URI of this template filling record." },
        "cid": { "type": "string", "format": "cid", "description": "CID of this template filling record's indexed revision." },
        "value": { "type": "ref", "ref": "pub.layers.resource.filling#main", "description": "The template filling record value." }
      }
    }
  }
}
