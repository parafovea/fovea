# Docker Compose configuration for E2E testing
# Lightweight stack for running Playwright E2E tests in CI
name: fovea-e2e

services:
  frontend:
    build:
      context: ./annotation-tool
      dockerfile: Dockerfile
      target: production
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - e2e-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3000/"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=test
      - DATABASE_URL=postgresql://fovea:fovea_password@postgres:5432/fovea_test
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - MODEL_SERVICE_URL=http://mock-model-service:8000
      - FOVEA_MODE=multi-user
      - DEFAULT_USER_USERNAME=default-user
      - DEFAULT_USER_DISPLAY_NAME=Default User
      - ALLOW_TEST_ADMIN_BYPASS=true
    volumes:
      - ./test-data:/data:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      mock-model-service:
        condition: service_started
    networks:
      - e2e-network
    command: sh -c "npx prisma migrate deploy && node dist/index.js"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3001/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  # Lightweight mock model service for E2E tests
  mock-model-service:
    build:
      context: ./test-utils
      dockerfile: Dockerfile.mock-model-service
    networks:
      - e2e-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:8000/health"]
      interval: 5s
      timeout: 3s
      retries: 5

  # Model service not needed for basic E2E tests
  # Can be enabled if tests require AI features
  model-service:
    build:
      context: ./model-service
      dockerfile: Dockerfile
      args:
        BUILD_MODE: minimal
        DEVICE: cpu
    ports:
      - "8000:8000"
    environment:
      - MODEL_CONFIG_PATH=/config/models.yaml
    volumes:
      - ./model-service/config:/config
    networks:
      - e2e-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    profiles: ["with-models"]  # Only start if explicitly requested

  postgres:
    image: pgvector/pgvector:pg16
    environment:
      - POSTGRES_USER=fovea
      - POSTGRES_PASSWORD=fovea_password
      - POSTGRES_DB=fovea_test
    ports:
      - "5433:5432"  # Different port to avoid conflicts
    networks:
      - e2e-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fovea"]
      interval: 5s
      timeout: 3s
      retries: 5
    tmpfs:
      - /var/lib/postgresql/data  # Use tmpfs for faster tests

  redis:
    image: redis:7-alpine
    ports:
      - "6380:6379"  # Different port to avoid conflicts
    networks:
      - e2e-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    tmpfs:
      - /data  # Use tmpfs for faster tests

networks:
  e2e-network:
    driver: bridge
